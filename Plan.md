# ChurchTools Infoscreen Designer – Projektplan

Ein ChurchTools Custom Module (CCM), mit dem angemeldete ChurchTools-Anwender Infoscreens gestalten. Das Ergebnis ist eine Webseite, die ein Kiosk-Browser aufruft und auf den Foyer-TVs anzeigt – auf einem Raspberry Pi, einem Mini-PC oder in einer VM.

## Auf einen Blick

**Stand 2026-09-25.** Der MVP ist gebaut und läuft im echten ChurchTools der Testinstanz: Designer mit Startseite nach dem Muster der Gruppen-Übersicht, Editor, Mediathek, Player, Einstellungen mit Einrichtungsassistent. **Die Fernseher melden sich über ihre Adresse selbst an (Weg B)** – Weg A trug nicht, weil jede Anmeldung nach 24 Stunden endet (G32). Das Repository ist **seit dem 2026-09-25 öffentlich**, der Release-Workflow steht, `docs/Einrichtung.md` führt fremde Gemeinden durch die Einrichtung.

**Als Nächstes:**

1. ~~**`v0.1.0`**~~ **veröffentlicht am 2026-09-25** als erstes Release. Jetzt: **der Test im Haus mit dem Release-ZIP** (Nutzer).
2. **Ein Raspberry im Foyer über mehrere Tage** – der eigentliche Praxistest, dabei die Laufschrift beobachten.
3. **Beiträge aus ChurchTools** (Punkt 28) – zuerst ein Testbeitrag, dann messen.
4. **Planen mit Opus, Umsetzen mit Sonnet** (AGENTS.md) über einige Aufträge beobachten: spart es, ohne dass die Qualität leidet?
5. **Vor dem 2026-10-22:** die Fixtures sichern (Modul-Antworten aufgezeichnet am 2026-09-25) und klären, wo nach Ablauf der Testinstanz getestet wird (F1).

**Bewusst geparkt** – nicht falsch, aber nicht jetzt: alles Weitere am Betriebsbenutzer (G21-Reste), Rate-Limit (G16), Heartbeat und Statusanzeige (widerspricht „Geräte nur lesend"), eine gemeinsame Bibliothek mit `ct-pass-store`, Koppeln am Fernseher.

## Rahmendaten

- **Testinstanz**: Adresse nur in der `.env`, Build 32882 wie produktiv, **Lizenz bis 2026-10-22, 21:53** (30 Tage ab Anlage am 2026-09-22, 21:53; T3). Custom Modules dort **freigeschaltet seit dem 2026-09-24** (T1), angefragt am 2026-09-23. Freigeschaltet wird nach Auskunft vom 2026-09-24 über die Entwickler von ChurchTools, nicht über das Paket; Rückmeldung steht aus. Ein Paketwechsel (etwa auf Combo) ist im Gespräch, dann werden Kalender und Personen der Testinstanz entsprechend reduziert. Die **Produktivinstanz** hat sie (G1).
- **Autor / Repo**: `wirsindcgks <media@cg-ks.de>`, [`github.com/wirsindcgks/churchtools-infoscreen`](https://github.com/wirsindcgks/churchtools-infoscreen), **öffentlich seit dem 2026-09-25**. Git und `gh` immer als `wirsindcgks`. Offen: ältere Commits nennen noch Instanz-Adressen und die Autor-Adresse – bereinigen oder so lassen, Entscheidung des Nutzers.
- **Lizenz**: GPL-2.0-or-later
- **Ziel**: Das Modul geht am Ende an die ChurchTools-Community – daher **kein Gemeinde-Branding** (seit 2026-09-24).
- **Extension-Key**: `infoscreen-designer` → Auslieferungspfad `/ccm/infoscreen-designer/`
- **Stack**: Vue 3 + TypeScript + Vite + Pinia, `@churchtools/churchtools-client`, Vitest, Playwright – wie das [Boilerplate](https://github.com/churchtools/extension-boilerplate) und die bekannten Fremd-Extensions.
- **Für Anwender** *(seit 2026-09-25)*: [`docs/Onboarding.md`](docs/Onboarding.md) – Einstieg je Rolle; [`docs/Einrichtung.md`](docs/Einrichtung.md) – die Anleitung für Administratoren; [`docs/Rechte.md`](docs/Rechte.md) – wer welche Rechte braucht, als Tabelle. **`docs/Rechte.md` wird mitgepflegt:** `src/setup/rights-doc.test.ts` bricht ab, sobald der Assistent ein Recht vergibt oder zurücknimmt, das dort nicht steht.
- **Dokumente**: `Plan.md` ist das Gedächtnis, [`Preparation.md`](Preparation.md) die Arbeitsliste, [`Befunde.md`](Befunde.md) das Messprotokoll (**alle Verweise „G…" zeigen dorthin**), [`AGENTS.md`](AGENTS.md) die Arbeitsregeln.
- **Abgrenzung**: Eigenständiges Projekt, kein Teil des WordPress-Plugins `churchtools-plugin` und keine gemeinsame Codebasis damit.

## Pitch

Der native ChurchTools-Infoscreen zeigt eine Terminliste in einem festen Layout. Wiederkehrende Wünsche im ChurchTools-Forum sind seit Jahren dieselben: eigene Inhalte (Bilder, Videos, freie Texte) im Wechsel mit den Terminen, ein abschaltbarer oder gestaltbarer Header, einstellbare Anzeigedauer, mehrere Screens mit unterschiedlichen Inhalten. Wer das braucht, weicht heute auf externe Digital-Signage-Dienste wie Yodeck aus – und pflegt seine Termine dann zweimal.

Der Infoscreen Designer schließt genau diese Lücke: gestalten in ChurchTools, Daten aus ChurchTools, ausspielen aus ChurchTools. Kein zweites System, kein zweiter Datenstand, keine zusätzliche Hosting-Rechnung.

**Dieser Absatz ist der Maßstab für jeden Punkt dieses Plans.** Was keines der Versprechen darin einlöst, gehört nach „Später".

## Architektur

Zwei Oberflächen, ein Paket, kein eigener Server.

```
ChurchTools-Instanz
├── /ccm/infoscreen-designer/            (das ausgelieferte dist/ der Extension)
│   ├── Designer   – angemeldeter Anwender gestaltet Screens
│   └── Player     – derselbe Code, Route /player?screen=<slug>
├── /api/custommodules/…             Screens + Einstellungen (KV-Store)
└── /api/…                           Termine, Dateien, Gemeindekopf
                     ▲
                     │ Kiosk-Gerät (Raspberry Pi, Mini-PC, VM – Chromium im Kioskmodus)
                     │ ruft /ccm/infoscreen-designer/player?screen=foyer-links&login_token=…&user_id=…
```

- **Kein eigener Server.** Die Instanz ist Speicher, Auslieferung und Rechteverwaltung zugleich. Der Preis: Der Pi meldet sich per Login-Token an (D) – so läuft der native Infoscreen heute schon.
- **Designer und Player teilen sich den Code.** Der Player rendert dieselben Blockkomponenten ohne Bedienelemente; die Vorschau im Designer ist damit definitionsgemäß das, was auf dem TV steht.
- **DOM statt Canvas**, auf einer Bühne mit festen Pixelmaßen (1920×1080 oder hochkant), die ein einziger Faktor per `transform: scale()` auf den Viewport abbildet. Passt das Seitenverhältnis nicht, wird **eingepasst statt beschnitten**; je Screen eine Overscan-Korrektur in Prozent.
- **Stilgrenze in beide Richtungen.** Die Extension läuft im Dokument der Hostseite, nicht in einem iframe (G6). Alle eigenen Regeln unter einer Wurzelklasse, `all: initial` an der Bühnenkante, keine geerbten Schriftgrößen oder Farben. Ungeschichtetes CSS schlägt die `@layer`-Stile der Hostseite, `!important` ist unnötig.
- **Der Player ist ein Bündel ohne nachzuladende Chunks** und lädt bei einem Modul-Ladefehler neu. Sonst zerlegt ein Extension-Update laufende Kiosk-Tabs mit einem `404`. Der Designer darf splitten.
- **Kein Inline-Skript im Build** – die CSP erlaubt `script-src` ohne `'unsafe-inline'` (G15).
- **Schriften kommen aus dem Paket**, nicht von Google Fonts: geschlossene Liste freier, selbst gehosteter Schriften (OFL), unter eigenen Namen registriert. Kein Aufruf eines Schriftendienstes – Datenschutz; `check-dist.js` prüft es.

## Voraussetzungen für die ChurchTools-Integration

### A. Auf Seiten der ChurchTools-Instanz

- **Custom Modules freigeschaltet** – produktiv ja (G1), auf der Testinstanz **nein** (T1).
- **Ein Administrator** zum Hochladen des Moduls und zum Vergeben der Modulrechte. **Direkt nach dem Hochladen die Rechte vergeben**: Ein frisch installiertes Modul ist auch für Administratoren unsichtbar (G4).
- **Ein Geräte-Benutzer** ohne Zweifaktor, mit Leserechten auf das, was die Screens zeigen – siehe F.
- **Kein CORS nötig**: In der Entwicklung proxt Vite `/api`, im Betrieb liegt die Extension auf derselben Domain.

### B. Entwicklungsumgebung

- **Node.js** (aktuelle LTS) mit npm, plus `zip` für die Paketierung.
- **Steht seit dem 2026-09-24** (B1–B4). Nach dem Muster des Boilerplates, aber an einer Stelle bewusst anders: **Der Dev-Login läuft im Vite-Proxy, nicht im Browser.** Der Proxy leitet `/api` an die Testinstanz und setzt dort `Authorization: Login <token>`; Set-Cookie-Kopfzeilen verwirft er. Damit gibt es kein `VITE_USERNAME`/`VITE_PASSWORD`, das ins Bündel geraten könnte, und der Safari-Fall (`Secure; SameSite=None` auf `localhost`) tritt gar nicht erst auf – belegt mit Playwright in WebKit.
- **`.env`** aus `.env-example`, nicht im Repo: `CT_BASE_URL`, `CT_LOGIN_TOKEN`, optional `VITE_KEY`. Die Anwendung nimmt `window.settings.base_url` der Hostseite, in der Entwicklung den eigenen Ursprung.
- **Befehle**: `npm run dev` (unter `/ccm/infoscreen-designer/`), `npm test` (ohne Instanz), `npm run smoke` (Playwright in Chromium und WebKit gegen die Testinstanz, nicht im CI), `npm run build` (inklusive `scripts/check-dist.js`), `npm run release` (ZIP).
- **Typ-Snapshot** `ct-types.d.ts` aus der Spezifikation **einer Instanz mit freigeschalteten Custom Modules** – die Spezifikation wird pro Benutzer gefiltert, ein Snapshot ohne `CustomModule`-Pfade ist schlimmer als keiner (B5).
- **Vorlage mit offenem Quellcode**: [`lub90/ct-pass-store`](https://github.com/lub90/ct-pass-store) (MIT) läuft auf unserer Produktivinstanz. `ct-utils/lib/ExtensionData.ts` (KV-Zugriff) und der Setup-Assistent für Kategorien sind übernehmbar, mit Urheberrechtsvermerk.

### C. Paketierung und Installation

- `npm run release` baut `dist/` und packt es nach `releases/<name>-v<version>-<commit>.zip` – nur `dist/`, ohne Source-Maps.
- Installation: Admin → Extensions → ZIP hochladen. **Der Kurzbezeichner muss exakt zum Build-Key passen**, Ordnernamen sind case-sensitiv.
- Updates über die Oberfläche, wenn ein Download-Link hinterlegt ist: Tag `vx.y.z` → GitHub Action baut das ZIP → Release-Asset ist die Update-Quelle.

### D. Authentifizierung

1. **Designer im Betrieb** – keine eigene Anmeldung; die Extension läuft in der ChurchTools-Sitzung, `GET /whoami` liefert den Anwender.
2. **Entwicklung** – `POST /login` mit den Zugangsdaten aus der `.env`, nur im Modus `development`.
3. **Fernseher – Weg B, gebaut am 2026-09-25:** Die Adresse trägt den Login-Token des Geräte-Benutzers zweimal – im Query, damit ChurchTools beim Laden anmeldet (G9), und im Fragment, das die Weiterleitung übersteht und der Player liest:
   ```
   https://<instanz>/ccm/infoscreen-designer/player?screen=foyer-links&login_token=<TOKEN>&user_id=<ID>#login_token=<TOKEN>&user_id=<ID>
   ```
   Der Player meldet sich bei abgelaufener Sitzung selbst neu an und lädt sich nur über diese Adresse neu; das nächtliche Neuladen erneuert die Sitzung täglich. Die Adresse erzeugt ein Administrator in den Einstellungen aus Benutzername und Passwort des Geräte-Kontos (`POST /api/login/token`, ohne Cookies); gespeichert wird nichts davon. Echte Unterpfade wie `/player` sind gedeckt (G7).
4. **Weg A, verworfen am 2026-09-25** (entschieden am 2026-09-24): Der Browser meldet sich einmal von Hand an, die Adresse trägt nur den Screen. Auch mit „Angemeldet bleiben" hält die Sitzung nur 24 Stunden (G32), danach bindet ChurchTools unser Skript nicht mehr ein (G9, G33). Bleibt zum Ausprobieren: angemeldet über „Player öffnen".

**Für Weg B gilt: Der Token ist ein Dauerpasswort** – in der URL, im Browserverlauf, auf der SD-Karte. **Die Notbremse ist der Passwortwechsel des Geräte-Benutzers in der Oberfläche**; der Token ist danach sofort ungültig (G18). Einen Admin-Endpunkt zum Widerrufen gibt es nicht.

### E. Datenspeicherung

Eigene Daten liegen im KV-Store des Moduls: Modul → Kategorie → Datenwert, jeder Wert ein JSON-String. **Ein Datenwert hat keinen Namen, keine Version und fasst höchstens 10.000 Zeichen** (G2, G3, G11). Daraus folgt der Zuschnitt:

| Kategorie | Inhalt |
| --- | --- |
| `screens` | je Screen ein Index-Wert: Slug, Name, Auflösung, Overscan, Schema-Version, Revision, `updatedAt` – gehört den Administratoren |
| `playlists` | je Playlist ein Wert: Name, Reihenfolge der Slide-IDs, **Format, Revision** (seit Schema 1.4 – eine Playlist steht für sich und kann auf mehreren Screens laufen); dazu **je Screen ein Zeitplan** (`kind: 'schedule'`, seit 1.2): Standard-Playlist, Regeln, eigene Revision – beides gehört den Gestaltern |
| `slides` | je Slide ein Wert mit ihren Blöcken (realistisch 1–4 KB). Slides gehören keiner Playlist, sie werden referenziert |
| `media` | Verweise auf ChurchTools-Dateien, nie die Dateien selbst |
| `settings` | modulweite Einstellungen |

Später hinzu: `templates`, `snippets` (Web-Code), `status` (Heartbeat).

**Regeln, die ab der ersten Zeile gelten:**

- **Gelesen wird eine Kategorie immer ganz**, gefiltert im Client – eine Abfrage nach Feldern gibt es nicht.
- **Die Adresse eines Screens ist sein Slug** (`foyer-links`), nicht die Server-`id`. Sonst zeigt die SD-Karte nach „löschen und neu anlegen" ins Leere.
- **Speichern schreibt mehrere Werte ohne Transaktion – Slides, dann Playlists, der Index zuletzt.** Neues bleibt bis zum Index unsichtbar, und kein Verweis zeigt ins Leere. Eine geänderte bestehende Slide ist allerdings sofort sichtbar (G22) – hingenommen.
- **Konflikte werden erkannt, nicht verhindert**: `revision` im JSON, vor dem Schreiben lesen und vergleichen, bei Abweichung ein Dialog statt stillen Überschreibens.
- **Die 10.000-Zeichen-Grenze prüft der Designer vor dem Speichern.** Damit ist es gleichgültig, ob der Server sauber ablehnt oder still kürzt (C4).
- **Validiert wird allein im Client**, gegen `src/model/schema.ts`. Kategorien haben auf unserem Build weder Schema noch Sicherheitsstufe (G22).
- **Persistierte Daten sind versioniert** und werden beim Lesen migriert. **Der Player ist duldsam gegenüber neueren Daten**: unbekannter Blocktyp wird übersprungen, unbekanntes Feld ignoriert, höhere Schema-Hauptversion führt zum Neuladen – nie zu einer leeren Slide. Das ist ein Test in Phase 1.
- **Die Kategorien legt die Extension beim ersten Aufruf selbst an**, wie der Setup-Assistent von `ct-pass-store`.
- **Der KV-Zugriff liegt hinter genau einer Naht** (Repository). Dahinter zuerst der Mock aus `fixtures/`, später der echte Store.

### F. Rechte und Datenschutz

**Für den MVP genügt ein Satz:** Der Geräte-Benutzer darf lesen, was die Screens zeigen, und nichts sonst. Konkret:

| Lesen auf | Wofür |
| --- | --- |
| Die Kalender, die ein Screen zeigt | Terminblöcke |
| `view custom data` auf `screens`, `playlists`, `slides`, `media`, `settings` | Screen-Konfiguration |

**Ausdrücklich nicht:** Personendaten, Schreibrechte, Administrationsrechte, Zweifaktor – **und auch kein Wiki-Recht**: Bilder kommen über die Adresse des Bilddienstes, die ohne Anmeldung trägt (G14, G27). **Rollenkonzept** *(festgelegt am 2026-09-24)*:

| Rolle | Wer | Darf |
| --- | --- | --- |
| **Administrator** | wer in ChurchTools Berechtigungen verwalten darf (`churchcore` „administer persons") | die Extension installieren; die **Einstellungen** öffnen, dort die Gruppen samt Rechten anlegen (Assistent) und prüfen; beim ersten Start entstehen die Kategorien des Moduls |
| **Gestalter** | Mitglieder von „Infoscreen-Designer" | **Inhalte und Zeitpläne**: Slides und Playlists anlegen, ändern, löschen; **wählen, welche Playlist wann läuft** (Zeitplan, seit 2026-09-25); Bilder in den Wiki-Bereich „Infoscreen" laden. **Nicht:** Screens anlegen, löschen oder konfigurieren (Name, Format, Overscan), Einstellungen, Kategorien, Rechte |
| **Gerät** | Mitglieder von „Infoscreen-Devices" | nur lesen: Modul, seine Daten, die Kalender der Screens |

Wer kein Administrator ist, sieht „Einstellungen" weder in der Kopfleiste noch in der Seitenleiste; ruft er die Adresse trotzdem auf, sagt die Seite, dass sie Sache der Administratoren ist. Das ist Bedienung, keine Sperre – die Sperre sind die Rechte selbst: Gestalter haben kein Schreibrecht auf `settings`, Geräte gar keines. **Entschieden am 2026-09-25: Screens konfigurieren nur Administratoren** – anlegen, löschen, Name, Format, Overscan –, damit Gestalter die Screen-Konfiguration nicht beschädigen können; ein Screen steht für ein Gerät im Haus. **Den Zeitplan wählen die Gestalter** (entschieden am selben Tag) – er gehört zum Inhalt, nicht zur Gerätekonfiguration. **Umgesetzt am 2026-09-25** (Punkte 15 und 17): Der Editor schreibt nur Slides, Playlists und das Zeitplan-Dokument; der Assistent gibt Gestaltern kein Schreibrecht mehr auf `screens` und nimmt ein vorhandenes zurück.

**Zwei Befunde, die den Bau betreffen – der Rest steht in [`Befunde.md`](Befunde.md), „Der Betriebsbenutzer":**

- **Fehlende Rechte sehen nicht wie Fehler aus**, sondern wie leere Listen (G20). Der Player belegt jede Anfrage mit `only_allow_authenticated=true`, prüft die Identität über `whoami` und **behandelt einen leeren Screen als Fehler**, nicht als leeren Kalender.
- **Wie das Konto eingerichtet wird, ist gemessen** (G21): Rechte über eine eigene Gruppe, Status „Aktiv", Rolle „Mitarbeiter"; die Person braucht einen Benutzernamen. Das reicht für den Bau. Die offenen Reste daran sind Stoff für die Einrichtungsdoku in Phase 4.

**Die Datenschutzfrage ist nicht die API, sondern das Foyer**: Was ein angemeldeter Gestalter in einen Screen legt, sieht jeder. Geburtstage, Dienstpläne mit Namen und Gruppenkontakte kommen deshalb nicht in den MVP.

## Phase 0 und Testinstanz

Das Messprotokoll steht in [`Befunde.md`](Befunde.md). Stand:

| | Punkte |
| --- | --- |
| **Beantwortet** | G1–G8, G11, G14, G15, G18, G19, G20, G22; G21 für den Bau ausreichend |
| **Blockiert und zwingend** | **G9** (Login-Token unter `/ccm/`) |
| **Blockiert, aber verzichtbar** | G10 (Service Worker – ohne ihn bleibt Offline-Festigkeit halb, siehe Risiko 2) |
| **Hinfällig** | G12, G13 – die Felder gibt es nicht (G22) |
| **Geparkt** | G16 (Rate-Limit) · G17 (Extension Store) · G21-Reste |

**Die Testinstanz** ist leer angelegt, Build und Upload-Grenze wie produktiv. Dort darf ausprobiert, liegengelassen und zerschossen werden; gegen die Produktivinstanz wird nur gelesen. **Ihr Ertrag sind die Fixtures unter `fixtures/`** (nicht versioniert) – gegen sie läuft die gesamte Entwicklung, und sie sind **seit dem 2026-09-24 außerhalb des Repos gesichert**. Neu Aufgezeichnetes wird vor Ablauf der Lizenz nachgesichert.

## Datenquellen für Inhaltsblöcke

Geprüft gegen die OpenAPI-Spezifikation 3.136.2. **Fett = MVP.**

| Block | Endpunkte |
| --- | --- |
| **Termine** | `/calendars`, `/calendars/appointments` (**`calendar_ids[]` Pflicht**, dazu `from`, `to`). Serien löst der Server auf, samt Ausnahmen und Zeitumstellung; Zeiten in UTC (G19) |
| **Gemeindekopf** | `/info` (Name, Anschrift), Logo über `<instanz>/logo`, anonym; Ziel der Weiterleitung mit `w`/`h` (G29) |
| **Bilder** | `imageUrl` (`/images/{fileId}/{hash}`) am Bilddienst, **immer mit `w` und `h`** (G14) |
| Beiträge / News | `/posts` – woran die Sichtbarkeit hängt, ist offen |
| Gruppen & Anmeldungen | `/groups`, `/publicgroups/{id}` |
| Raumbelegung | `/resource/masterdata`, `/bookings` (`resource_ids[]` Pflicht) |
| Dienste / Gottesdienst | `/events`, `/events/{id}/agenda`, `/services` |
| Freie Texte | `/wiki/pages` |

## Medien

**Gebaut am 2026-09-24 (G8, G14, G26, G27):** Das Modul hat einen eigenen Wiki-Bereich **„Infoscreen"**. Seine Startseite `main` erklärt Designer und Einrichtung und wird vom Modul geschrieben, solange sie niemand ändert. **Jeder Screen hat eine Seite, benannt nach seiner Adresse**; an ihr hängen die für ihn hochgeladenen Bilder, verwendbar sind sie in jedem Screen. Seite und Bereich legt der Designer bei Bedarf selbst an. Jede Datei trägt eine `imageUrl` am Bilddienst – der Player fordert damit genau die Blockgröße an. Ohne Wiki gibt es keinen sauberen Weg (G26).

Die Mediathek im Designer lädt hoch (Knopf oder Ziehen), zeigt alle Bilder aller Screen-Seiten und übernimmt auch Bilder, die jemand direkt im Wiki hochgeladen hat. **Vor dem Löschen nennt sie jede Stelle, an der ein Bild gezeigt wird.**

- **Beide Maße sind Pflicht**: Ohne Parameter liefert der Bilddienst 150×150, `w` allein ergibt 1920×150.
- **Die `imageUrl` ist anonym abrufbar**, geschützt allein durch den Hash – gehört in die Betriebsdoku.
- **Beim Upload im Browser herunterskalieren**, auf 3840 px an der langen Kante – ChurchTools verkleinert selbst nichts, `max_width` bleibt ohne Wirkung (G27).
- **Eine fehlende Datei zeigt der Player als ruhigen Platzhalter**, nicht als Bruchsymbol.
- **Videos kommen nach dem MVP**: Externe Videos blockiert die CSP (kein `media-src`, G15), und ein Pi spielt sie nicht zuverlässig ab. Erst auf der echten Hardware messen.

## Eigener Web-Code – nach dem MVP

Ein HTML-Block führt fremden Code auf der ChurchTools-Domain aus, in der Sitzung eines angemeldeten Benutzers. Er ist das riskanteste Stück des Moduls, und seit G15 ist er zusätzlich beschnitten: **Ein `srcdoc`-Rahmen erbt die CSP, eigene `<script>`-Schnipsel laufen darin nicht.** Was trägt, ist „fremde Seite per `src` einbetten" sowie reines HTML/CSS. Deshalb **nicht im MVP** (entschieden am 2026-09-24, siehe Offene Entscheidung 2).

**Wenn er kommt, gilt ohne Ausnahme:** `<iframe sandbox="allow-scripts">` **ohne** `allow-same-origin`, abgesichert durch einen Test; eigenes Recht, getrennt vom Gestalten; eigener Datenwert in `snippets`; Code wird unverändert gespeichert – die Sandbox ist die Grenze, kein Filter.

## Playlists und Zeitpläne

**Die Oberfläche ist seit dem 2026-09-25 gebaut (Punkte 17 und 19 der Nächsten Schritte); wählen dürfen sie die Gestalter.** **Entschieden am 2026-09-25: Playlists stehen für sich** – sie werden unabhängig von einem Screen angelegt, und ein Screen wählt sie in seinem Zeitplan; dieselbe Playlist darf auf mehreren Screens laufen. Der Editor hängt an der Playlist, nicht am Screen. Begriffe: **Screen** = Gerät (Administratoren), **Zeitplan** = was der Screen wann zeigt (Gestalter, am Screen), **Playlist** = Inhalt (Gestalter, eigener Bereich). **Entschieden am 2026-09-23:** Das Modell lautet **Screen → Playlist → Slides**. Die Ebene kommt in Phase 1 ins Schema, weil sie sich später nur mit einer Datenmigration auf laufenden Geräten nachziehen ließe. **Die Oberfläche dafür kommt später.** Im MVP hat jeder Screen genau eine Playlist, und der Gestalter merkt davon nichts.

Für den späteren Zeitplan liegen die Regeln fest, damit das Schema sie tragen kann:

- Zwei Regelarten: **nach Termin** („30 Minuten vor Gottesdienstbeginn" – dank G19 billig) und **nach Uhrzeit**.
- **Eine Standard-Playlist ist Pflicht**, dazu eine feste Vorrangregel bei Überschneidungen.
- **Der Player wechselt die Playlist nicht, solange seine Uhr unbestätigt ist**, und hält alle Playlists eines Screens vor.
- **Slides werden referenziert**, dürfen in mehreren Playlists stehen; der Designer zeigt, wo überall.

## Funktionsumfang

### MVP – was der Pitch verlangt

**Designer**

- Screens anlegen, umbenennen, löschen; je Screen ein **Slug** und eine Auflösung (quer oder hoch).
- Slides mit **eigener Anzeigedauer**, Reihenfolge per Drag-and-drop, einzeln deaktivierbar.
- Blöcke frei platzierbar: **Terminliste, Einzeltermin, Bild, Text, Fläche/Farbverlauf, Uhr und Datum, Gemeindekopf** (der abschaltbare Header aus dem Pitch).
- Gestaltung: Position, Größe, Schrift, Farben, Hintergrund, Ebenen.
- Bindung eines Terminblocks: Kalenderauswahl, Zeitfenster, Anzahl.
- **Mediathek**: hochladen und auswählen.
- Live-Vorschau in Zielauflösung, unter denselben Bedingungen wie der Player.

**Player**

- Route `/player?screen=<slug>`, keine Bedienelemente, kein Mauszeiger, alles stumm.
- Bühnenskalierung mit Letterbox und Overscan-Korrektur.
- Slide-Rotation; **drei getrennte Intervalle**: Slides (Sekunden), ChurchTools-Daten (Minuten), Screen-Konfiguration (wenige Minuten – sonst sieht niemand seine Änderung, bevor er das Haus verlässt).
- Anmeldung per Token, `whoami`-Prüfung, leerer Screen = Fehler mit sichtbarer Meldung (F).
- **Uhrprüfung**: Gerätezeit gegen die Zeit einer API-Antwort; unbestätigt lieber keine Uhr als eine falsche.
- Letzter erfolgreicher Stand in IndexedDB, **Bilder in Cache Storage** (nach dem ersten Durchlauf lädt das Gerät sie von der eigenen Platte, G28), Bilder der nächsten Slide vorab dekodiert; Backoff bei Fehlern, `429`/`Retry-After` beachten, zufälliger Versatz im Intervall.
- Selbstheilung: Neuanmeldung, Neuladen nach wiederholten Fehlern und bei Modul-Ladefehlern, nächtlicher Neustart der Seite.
- Duldsam gegenüber neueren Daten (E).

### Später

In ungefährer Reihenfolge des Nutzens:

- **Updates der Extension**: Auf Build 32882 nur als ZIP im Bearbeiten-Dialog der Extension-Verwaltung – ein Feld für eine Download-Adresse oder ein automatisches Update aus GitHub gibt es nicht (Code der Oberfläche gelesen, 2026-09-24). Releases auf GitHub tragen deshalb das ZIP zum Herunterladen und Hochladen.
- Heartbeat und Statusanzeige je Screen („zuletzt gesehen") – braucht die Kategorie `status` und damit das einzige Schreibrecht des Geräts. **Widerspricht dem Rollenkonzept** (Geräte nur lesend, F) – vor dem Bau entscheiden.
- **Anregungen aus einer anderen Digital-Signage-Oberfläche** *(vom Nutzer gezeigt am 2026-09-24; ein fremdes, echtes Produkt – Ideen übernehmen, Aussehen, Texte und Namen nicht)*: auf der Kachel ein **Status-Punkt** je Screen (setzt den Heartbeat voraus, siehe oben); die Vorschau **in einem stilisierten Bildschirm** statt als nacktes Bild; ein **Umschalter „läuft gerade"** direkt auf der Kachel, der die Standard-Playlist wechselt, ohne den Editor zu öffnen; **eigene Bereiche** für Playlists, Zeitpläne und die Mediathek in der Navigation, sobald Playlists mehr als eine Liste je Screen sind; Auswertungen als fernes „vielleicht".
- **Koppeln am Fernseher** *(geprüft und zurückgestellt am 2026-09-25)*: Adresse ohne Token, einmal am Gerät als Geräte-Benutzer anmelden, der Player holt sich seinen eigenen Token (`GET /persons/{eigene id}/logintoken`, für das eigene Konto `200`, G18) und lädt sich danach nur über die Adresse mit Token neu – der Administrator gäbe das Geräte-Passwort nie im Designer ein. **Zurückgestellt, weil:** die Startadresse im Kiosk ohne Token bleibt; ist das Gerät länger als etwa einen Tag aus, bindet ChurchTools unser Skript nicht mehr ein (G33) und jemand muss sich am Gerät neu anmelden. Die Raspberries der Gemeinde haben **weder Tastatur noch Maus**, und der im Foyer wird zum Umstellen auf dem Rollwagen abgeschaltet. Der Generator (Weg B, D) trägt alle Fälle mit einem Weg.
- Export/Import eines Screens als JSON, Vorlagen, Duplizieren.
- Weitere Datenblöcke: Beiträge, Gruppen/Anmeldungen, Raumbelegung, Dienste. QR-Code, Laufschrift.
- Wechsel der Auflösung eines bestehenden Screens als Umrechnung mit Vorschau.
- Löschen in der Mediathek mit Referenzzählung (bis dahin: löschen in ChurchTools, der Player zeigt einen Platzhalter).
- Warnung bei schlechter Lesbarkeit aus fünf Metern.
- Service Worker für den Neustart ohne Netz (G10).
- Web-Code-Block, Videos.
- Extension Store (G17). Das Rollenkonzept steht seit dem 2026-09-24 (F).

## Phasen

| Phase | Inhalt | Ergebnis |
| --- | --- | --- |
| **0 – Machbarkeit** | **Abgeschlossen am 2026-09-24:** Extension hochgeladen, läuft im echten ChurchTools; G9 wird nur für Weg B gebraucht | „Hallo &lt;Vorname&gt;" läuft im echten ChurchTools ✓ |
| **1 – Datenmodell** | Schema Screen → Playlist → Slides → Blöcke, versioniert und duldsam; Slug; KV-Repository mit Mock; Revisionsprüfung. **Dazu die Terminnormalisierung** als eigene Schicht mit Tests gegen die Fixtures (ganztägig, mehrtägig, Zeitzone der Instanz) | Screens lassen sich ohne Oberfläche speichern und laden |
| **2 – Player** | Blockrendering, Bühne, Rotation, Intervalle, Token-Anmeldung, Uhrprüfung, Offline-Stand | Ein von Hand geschriebener Screen läuft auf einem Kiosk-Gerät am Foyer-TV |
| **3 – Designer** | Editor, Slide-Verwaltung, Blockpalette, Inspektor, Vorschau, Mediathek | Ein Screen mit eigenen Bildern entsteht ohne Entwicklerhilfe |
| **4 – Betrieb** | Einrichtungsdoku für Geräte-Benutzer und FullPageOS, Release-Workflow | Ein zweiter Screen geht ohne uns in Betrieb |

Phase 2 vor Phase 3 – ein Designer für ein Ausgabeformat, das noch nie auf einem TV lief, gestaltet ins Blaue.

## Konventionen

Sprache, Geheimnisse, Commit-Form und die drei Bauregeln stehen in [`AGENTS.md`](AGENTS.md). Hier nur, was dort fehlt:

- **Tests laufen nie gegen eine Live-Instanz**, sondern gegen aufgezeichnete Antworten unter `fixtures/`. Das Verzeichnis ist nicht versioniert; ein frisch geklonter Arbeitsplatz muss sie sich beschaffen.
- **Ohne Fixtures überspringen sich die Tests, die sie brauchen** – sichtbar als übersprungen, nicht still grün (entschieden am 2026-09-24). Das CI auf GitHub hat keine Fixtures und prüft deshalb nur, was ohne sie geht. **Die Form der Antworten wird dafür dokumentiert statt eingecheckt**: als Typen im Code, die nur die tatsächlich gelesenen Felder beschreiben, ohne Daten. Tests ohne Fixtures bauen ihre Eingaben aus diesen Typen selbst.
- **Aufgezeichnet wird nur von der Testinstanz – und vor dem Ablegen bereinigt**, mit `scripts/sanitize-fixture.js`. Zu entfernen sind mindestens:

  | Klasse | Beispiel | Warum |
  | --- | --- | --- |
  | Personenbezogene Felder | E-Mail, Telefon, Anschrift, Geburtstag | Datenschutz |
  | Instanz-URL | `site_url`, `fileUrl`, `apiUrl` | Grundregel des Repos |
  | Schlüssel und Geheimnisse | `site_licensekey`, `*_apikey`, `*_token`, `*_secret` | Beim ersten Durchgang übersehen |
  | Geheime Abo-Adressen | `randomUrl` am Kalender, `iCalUid` | Die iCal-Adresse ist ein Zugangsschlüssel (G22) |
  | Datei- und Bild-Hashes | `/images/{id}/{hash}`, `filename=<hash>` | Zugangsschlüssel, keine Kennungen (G14) |

  Die Regel gilt auch für Felder, die auf der Testinstanz leer sind – auf einer produktiven Instanz sind sie es nicht unbedingt. **Nicht** bereinigt werden Gruppen-, Kalender- und Dienstnamen: Umlaute, Längen und Namensgleichheiten sind wertvolle Testdaten.
- **Tests, die Vorgaben sichern**: Duldsamkeit des Players (erfundener Blocktyp, unbekanntes Feld). Genau ein JS-Bündel, kein Inline-Skript und keine `*.church.tools`-Adresse im `dist/` prüft `scripts/check-dist.js` bei jedem Build.
- **CI bei jedem Push**: Lint, Typecheck, Tests, Build. Tag `vx.y.z` baut das Release; Version in `package.json`, `package-lock.json` und `CHANGELOG.md` gemeinsam ziehen.

## Risiken

1. **Die Freischaltung kommt nicht rechtzeitig.** Die Lizenz der Testinstanz läuft am 2026-10-22 um 21:53 ab, die Anfrage ist unbeantwortet. Bis Phase 2 hält das nichts auf – dann aber muss ein Modul irgendwo laufen. Rückfall ist ein Testmodul unter eigenem Key (`infoscreen-designer-test`) auf der **Produktivinstanz**, das nur in eigene Kategorien schreibt. **Das ist eine Entscheidung, keine Automatik** – nach den Arbeitsregeln wird dort bisher nichts geschrieben.
2. **Das Gerät läuft unbeaufsichtigt.** Speicherlecks, abgelaufene Sitzungen, Netz- und Stromausfälle, schwache Hardware. Der Player muss von selbst wieder hochkommen. Daten und Bilder liegen nach dem ersten Durchlauf auf dem Gerät (G28); die Seite selbst aber nicht – ohne Service Worker (G10) zeigt ein Gerät, das während eines Netzausfalls neu startet, nichts. Das ist dann zu benennen, nicht zu übergehen.
3. **Login-Token auf der SD-Karte.** Tragbar, weil der Rückzugsweg gemessen ist (Passwortwechsel, G18) und das Konto nur liest.
4. **Stilgrenze zur Hostseite.** Ohne sie hängt das Aussehen der Bühne von ChurchTools-Updates ab – und unsere Stile beschädigen fremde Oberflächen.
5. **ChurchTools ändert die Extension-Schnittstelle.** Deshalb eine Naht (Repository), nicht KV-Zugriffe quer durch die Anwendung.
6. **Stilles gegenseitiges Überschreiben.** Kein ETag, keine Transaktion (G3). Revisionsprüfung und „Index zuletzt" erkennen den Konflikt, verhindern ihn nicht.
7. **Ein Update der Extension bricht laufende Player.** Neue Asset-Namen, alter Kiosk-Tab, `404` auf einen Chunk. Deshalb ein Bündel und Neuladen bei Modul-Ladefehlern, im CI geprüft.
8. **Die Instanz-URL landet im Release-Build.** Vite ersetzt `import.meta.env.*` zur Bauzeit. Deshalb tragen Instanz-URL und Token kein `VITE_`-Präfix und leben nur im Dev-Proxy; `scripts/check-dist.js` sucht bei jedem Build nach `*.church.tools` im `dist/`.

## Offene Entscheidungen

**Vor Phase 1 – beide entschieden:**

1. ~~**Undo/Redo: Zustand oder Befehle?**~~ – **entschieden am 2026-09-24 für V1: Zustand.** Der Designer führt einen Verlauf von Schnappschüssen der betroffenen Slide, keine umkehrbaren Befehle. Die Werte sind klein (höchstens 10.000 Zeichen), und Schnappschüsse sind mit Pinia einfacher und robuster. Reicht das später nicht mehr, wird neu entschieden.
2. ~~**MVP-Zuschnitt**~~ – **entschieden am 2026-09-24: wie unter „Funktionsumfang – MVP"**. V1 hat Termine, Bilder, Text, Gemeindekopf und Uhr, eine Playlist je Screen; kein Web-Code-Block, keine Geburtstage, keine Videos. Alles unter „Später" ist ausdrücklich nicht V1.

3. ~~**Hardware**~~ – **entschieden am 2026-09-24: keine Zielhardware.** Die Extension geht an die Community; dort laufen Raspberry Pis, Mini-PCs und VMs nebeneinander. Der Player setzt deshalb nur einen aktuellen Browser mit **dauerhaftem Profil** voraus und holt nach dem ersten Durchlauf alles Schwere vom Gerät selbst: Konfiguration und Termine aus IndexedDB, Bilder aus Cache Storage, die Bilder der nächsten Slide vorab dekodiert. Ein Test auf schwacher Hardware (ein älterer Pi) bleibt sinnvoll, entscheidet aber nichts mehr vorab.

**Nicht blockierend, aber offen:**

4. **Zeitbudget.** Der Plan nennt Phasen, aber keine Schätzung; für ein Feierabendprojekt entscheidet das über den Zuschnitt.

**Mit Vorgabe beantwortet, bis jemand widerspricht:**

- **Zielgruppe**: zuerst nur wir; der Extension Store (G17) kommt später. Keine eigene Instanz-URL im Build hält die Tür offen.
- **Wer gestaltet**: zuerst wir.
- **Aktualität**: Konfiguration alle 2 Minuten, Daten alle 10 Minuten, jeweils mit Versatz.
- **Ton**: alles stumm.

**Gestrichen am 2026-09-24:** die Rückfallposition bei den Medien (der Wiki-Weg trägt) und eine gemeinsame `ct-utils`-Bibliothek mit `ct-pass-store` (Abstimmungsaufwand ohne Produktnutzen; der Code bleibt als Vorlage lesbar).

## Nächste Schritte

**Zwingend, in dieser Reihenfolge:**

1. ~~**Fixtures außerhalb des Repos sichern**~~ – **erledigt am 2026-09-24.**
2. ~~**Ablaufdatum der Testinstanz nachsehen**~~ (T3) – **2026-10-22, 21:53.** Das ist der Stichtag für Risiko 1.
3. ~~**Entwicklungsumgebung**~~ (`Preparation.md` B1–B4) – **erledigt am 2026-09-24**, „Hallo &lt;Vorname&gt;" läuft in Chromium und WebKit.
4. **Phase 1** – **Kern steht seit dem 2026-09-24**: Schema mit duldsamem Lesen, Repository mit Mock und Revisionsprüfung, Terminnormalisierung samt Zeitzone. Ganztägige Termine sind seit G23 gemessen. Offen: die echte KV-Anbindung gegen eine Instanz prüfen (nach T1).
5. **Phase 2** – **Player läuft seit dem 2026-09-24 gegen den Mock**, mit echten Terminen der Testinstanz: Bühne mit Letterbox und Overscan, Rotation, Zeitplan-Auswertung, drei Intervalle mit Versatz und Backoff, Zeitlimit je Anfrage, Uhrprüfung über den `Date`-Kopf, letzter Stand in IndexedDB, Bilder in Cache Storage mit Vorab-Dekodierung der nächsten Slide, Neuladen bei neuerem Schema und nachts. Im Designer eine Screen-Liste mit Link in den Player; ohne Custom Modules steht dort in der Entwicklung ein Demo-Screen aus dem Arbeitsspeicher, im Release-Bündel fehlt er. **Offen:** Anmeldung per Token unter `/ccm/` (G9), das Logo im Kopfblock, mitgelieferte Schriften statt Systemschriften, Service Worker (G10) und ein Test auf schwacher Hardware (Offene Entscheidung 3).
6. **Phase 3** – **Editor steht seit dem 2026-09-24**: Screens anlegen (Name, feste Adresse, quer oder hochkant) und löschen; Slides hinzufügen, duplizieren, entfernen, per Ziehen ordnen, Dauer und Hintergrund setzen, abschalten; alle Blocktypen einfügen (damals sieben, heute neun), auf der Bühne ziehen und an acht Griffen skalieren – mit einblendbarem Raster (10–60 px) zum Einrasten und Hilfslinien an Bühnenmitte, Bühnenrändern und anderen Blöcken, aussetzbar mit der Alt-Taste – im Inspektor gestalten, Kalender wählen, Ebenen ordnen. Rückgängig/Wiederholen als Schnappschüsse des ganzen Screens – ein Ziehen oder ein Feld ist ein Schritt. Speichern mit Revisionsprüfung und Dialog bei Konflikt, Warnung beim Verlassen mit ungespeicherten Änderungen. Die Vorschau nutzt die Komponenten des Players mit Live-Daten. **Im Demo-Modus** (Entwicklung ohne Custom Modules) teilen sich Designer- und Player-Tabs eines Browsers denselben Speicher, und ein offener Player übernimmt Gespeichertes sofort; im Betrieb findet er Änderungen beim nächsten Konfigurationsabruf (Vorgabe 2 Minuten). Nach einer Änderung bleibt der Player auf der gezeigten Slide, statt von vorn zu beginnen. Aussehen nach den Tokens der Hostseite (G25). Die **Mediathek** steht seit demselben Tag: Upload in den Wiki-Bereich „Infoscreen", Bild für Bildblock und Slide-Hintergrund, Löschschutz mit Nennung der Verwendungen. Lesbarkeitswarnung und Vorlagen stehen unter „Später", nicht im MVP.

**Bis zum MVP, nach Priorität** *(festgelegt am 2026-09-24)*:

1. ~~**Player an den Geräte-Benutzer binden.**~~ **Erledigt am 2026-09-24.** Hat der Kiosk-Browser noch die Sitzung eines Menschen, liefe der Player mit dessen Rechten, und der Token würde nie benutzt. Weicht `whoami` von `user_id` ab, meldet er sich jetzt mit dem Token neu an; gelingt das nicht, zeigt er einen Fehler mit beiden Personennummern, ohne Namen. Ein solcher Fehler sperrt auch die Datenabrufe, bis die Anmeldung wieder stimmt – sonst hätte der nächste Datenabruf ihn überdeckt, wo ein gespeicherter Stand vorlag. **Ungemessen:** ob `login_token` eine bestehende fremde Sitzung tatsächlich ersetzt (G9).
2. ~~**Neuladen nach anhaltenden Fehlern**~~ **Erledigt am 2026-09-24:** nach 30 Minuten ununterbrochener Fehler eines Abrufs – aber nur, wenn die Seite selbst erreichbar ist. Ohne Service Worker (G10) ersetzte ein Neuladen im Netzausfall den alten Stand durch die Fehlerseite des Browsers. Scheitert schon das Laden des Bündels, hilft kein eigener Code; das gehört als Kiosk-Einstellung in die Einrichtungsdoku.
3. ~~**Fehlende Rechte im Designer benennen**~~ **Erledigt am 2026-09-24.** Die Startseite liest `/permissions/global` und nennt, was fehlt: Modulrechte je Kategorie (Schreibrechte nur auf Screens, Playlists, Slides und Medien – die Einstellungen schreibt die Einrichtung) und die Wiki-Rechte am Bereich „Infoscreen". Eine unsichtbare Kategorie wird mit beiden möglichen Ursachen benannt, weil die API sie nicht unterscheidet (G20). **Ungeprüft bis zur Freischaltung:** die Form der Modulrechte (G4 aus der Spezifikation, nicht aus einer Antwort) und ob „alle Kategorien" als Liste oder anders geliefert wird.
4. ~~**Risiko 1 entscheiden**~~ **Entfallen am 2026-09-24:** Custom Modules sind auf der Testinstanz freigeschaltet (T1). Offen bleibt die Laufzeit der Testinstanz über den 2026-10-22 hinaus (F1).
5. ~~**Logo im Gemeindekopf**~~ **Erledigt am 2026-09-24.** Player und Vorschau lösen `<instanz>/logo` bei jedem Datenabruf auf und fordern das Ziel in der Größe des Blocks an (G29); das Logo liegt wie alle Bilder im Gerätespeicher, ein neues Logo kommt über die neue Adresse von selbst. Scheitert der Abruf, bleibt das letzte Logo. Im Inspektor „Logo zeigen" und ein eigenes Logo aus der Mediathek als Ersatz, etwa für ein dunkles Logo auf dunklem Grund – als optionales Feld, Schema 1.1; der Löschschutz der Mediathek kennt es. Neue Kopfblöcke zeigen das Logo von Anfang an. **Ungemessen:** was `/logo` ohne hinterlegtes Logo liefert – alles außer einem Bild gilt als „kein Logo".
6. ~~**Mitgelieferte Schriften**~~ **Erledigt am 2026-09-24:** zehn freie Schriften unter der SIL Open Font License, alle von der eigenen Instanz ausgeliefert, nie von einem Schriftendienst – das wäre ein Datenschutzproblem, weil jeder Aufruf Gerät und Gestalter einem Dritten verriete. Ausgewählt nach Verbreitung (Web Almanac 2024, lateinische Schriften ohne Icon-Schriften): Roboto, Open Sans, Poppins, Montserrat, Lato, Source Sans 3, Inter, Oswald; dazu Barlow Semi Condensed und als Serifenschrift Merriweather statt Raleway, deren Mediävalziffern Uhrzeiten unruhig machen. Lato ist Standard, wie im eingebauten Infoscreen (G25). Registriert unter eigenen Namen („ISD …"), nur Latin und Latin-ext, nur 400/600/700; geladen wird eine Schrift erst, wenn Text sie braucht. Ziffern sind auf der ganzen Bühne gleich breit (`tabular-nums`), damit die Uhr beim Minutenwechsel nicht zuckt – gemessen wirkt das in acht der zehn Schriften, Oswald und Poppins bringen keine gleich breiten Ziffern mit. Die alten Schlüssel `sans`, `serif`, `mono` zeigen auf Lato und Merriweather, unbekannte auf Lato. `check-dist.js` bricht ab bei einer fremden Schriftquelle und bei Schriftdateien ohne Lizenztexte. **Offen:** eine Hausschrift als Modul-Einstellung (mit Punkt 8), eine eigene hochgeladene Schrift (Später – das Gerät bräuchte Wiki-Rechte).
7. ~~**Umbenennen abrunden**~~ **Erledigt am 2026-09-24:** Der Name steht im Inspektor unter „Screen" (kein Block gewählt); beim Speichern wird er getrimmt, ein leerer wird mit Hinweis abgelehnt. Im Schema bleibt ein leerer Name lesbar, damit ältere Werte nicht verloren gehen.
8. ~~**Einrichtungsseite, Stufe 1**~~ **Erledigt am 2026-09-24** (`/einrichtung`, verlinkt von der Startseite): je eine Gruppe für Gestalter und Geräte wählen, gespeichert in `settings`; die Seite prüft Status, Mitglieder und Rechte und nennt, was fehlt – sie ändert selbst keine Rechte. Gestalter: Wiki-Rechte am Bereich „Infoscreen" je Rolle mit Mitgliedern. Geräte: je Mitglied alle Quellen zusammen (Gruppenrolle, Gruppentyp-Rolle, Personenstatus, direkte Rechte), Kalender der Screens sichtbar oder öffentlich, keine überflüssigen Wiki-Rechte. Kernrechte als Konstanten aus G30. **Offen:** die Rechte am Modul selbst (sobald die Extension installiert ist), Rechte aus anderen Gruppen zählen nicht mit, die Hausschrift. Dazu die **Adressen für die Fernseher** (Weg A, D) mit Kopier-Knopf.
9. ~~**Einrichtungsassistent**~~ *(gewünscht am 2026-09-24 – die Rechteverwaltung von ChurchTools ist für Gemeinden zu kompliziert)*: Auf der Einrichtungsseite legt ein Administrator mit einem Knopf zwei leere, aktive Gruppen vom Typ „Merkmal" an – **„Infoscreen-Designer"** und **„Infoscreen-Devices"** – und gibt ihren Rollen die Rechte: Designer Modul sehen, Kategorien sehen, Daten sehen/anlegen/bearbeiten/löschen, Wiki-Bereich „Infoscreen" sehen und bearbeiten; Devices Modul sehen, Kategorien und Daten sehen, „Einzelnen Kalender sehen" für die Kalender der Screens. Vorher zeigt die Seite, was sie tun wird; sie fasst nur Gruppen an, die sie selbst angelegt hat; „Recht ergänzen" für neue Kalender, „Einrichtung entfernen" als Rückweg. Die Rechte-Nummern des Moduls liest sie zur Laufzeit (G33). Gemessen ist das Anlegen und Löschen von Gruppen (G34), **das Setzen von Rechten nicht** – das testet der Nutzer mit dem fertigen Knopf auf der Testinstanz. **Gebaut am 2026-09-24, auf der Testinstanz vom Nutzer ausgelöst – die Rechte kamen wie geplant an (G34):** Karte „Automatisch einrichten" auf der Einrichtungsseite, sichtbar für alle, auslösbar nur mit „Berechtigungen verwalten"; Rechte-Nummern des Moduls aus dem Katalog (`oldApi`, nur innerhalb von ChurchTools); Daten-Rechte auf die fünf Kategorien einzeln, wie ChurchTools sie selbst speichert; der Wiki-Bereich wird bei Bedarf angelegt; nach einem Abbruch bleibt das Angelegte in `settings.createdGroupIds` und damit entfernbar. Die Ampel prüft seitdem auch die Modulrechte – genau die, die der Assistent vergibt. Im Demo-Modus erklärt er nur. Neue Kalender ergänzt „Rechte aktualisieren".
10. ~~**Startseite überarbeiten**~~ **– nach dem Muster der Gruppen-Übersicht von ChurchTools** *(gewünscht am 2026-09-24, Entwurf bestätigt am selben Tag)*: Auf der alten Startseite fand man sich schlecht zurecht, Screens waren nur Textzeilen, die Formularfelder ungleich hoch. Der Aufbau folgt „Gruppen": **Kopfleiste** mit „Screens | Einrichtung" und einem grünen „+ Screen erstellen" (`--color-constructive-*`, G25); **Seitenleiste** mit Übersicht, Filter nach Format (quer, hochkant, je mit Symbol und Anzahl) und Verwaltung (Einstellungen, nur für Administratoren); **Suchfeld**; **Kacheln** mit einer echten Vorschau der ersten Slide im richtigen Seitenverhältnis, Name, Format-Symbol, Adresse, Anzahl der Slides und wer zuletzt gespeichert hat; ein Klick öffnet den Editor, ein „…"-Menü bietet Player, Adresse kopieren und Löschen. „Screen erstellen" wird ein **Dialog**, die Ausrichtung zwei Schaltflächen mit Symbol statt eines Auswahlfelds. „Hallo &lt;Vorname&gt;" entfällt – den Namen zeigt ChurchTools oben rechts. Die Bausteine der ChurchTools-Oberfläche sind nicht öffentlich (G24); nachgebaut wird der Aufbau, Farben und Schrift kommen über die Tokens (G25). Symbole als Inline-SVG, keine Symbolschrift (CSP, `font-src 'self'`). **Gebaut am 2026-09-24** – mit drei Änderungen gegenüber dem Entwurf: „Einrichtung" heißt jetzt **„Einstellungen"** wie bei den Gruppen (Adresse `/einstellungen`, die alte leitet weiter); die Seitenleiste steht wie in ChurchTools **auch auf der Einstellungsseite**, die Filter sind Links (`?format=portrait`); die **Adresse für den Fernseher** steht nicht mehr in den Einstellungen, sondern gehört zum Screen – **vorläufig** im Inspektor des Editors (Abschnitt „Screen"), samt Anleitung und Kopier-Knopf; ihr endgültiger Platz ist Punkt 15. Die Kacheln lesen alle Kategorien einmal (`listScreenOverviews`), nicht je Screen. **Handy-tauglich** *(gewünscht am 2026-09-24 – manche Gestalter arbeiten am Telefon)*: Breakpoints wie ChurchTools (40/48/64 rem); unter 48 rem wird die Seitenleiste zu einer waagerecht wischbaren Filterzeile, die Kacheln laufen ein- oder zweispaltig, Dialoge füllen die Breite, Tippflächen mindestens 40 px. **Gleich mit übernommen in Editor und Einrichtung:** dieselbe Kopfleiste (Zurück-Weg, Titel, Aktionen rechts), dieselben Knöpfe, Karten und Dialoge. **Der Editor am Handy** bekommt dabei nur eine Grundordnung: Slides als Leiste oben, Bühne, Inspektor darunter – Gestalten mit dem Finger (Ziehen, Griffe) ist Teil von Punkt 11.
11. ~~**Designer leichter bedienbar**~~ **Gebaut am 2026-09-25** *(gewünscht am 2026-09-24)*: **Farben als Hex-Wert** – jedes Farbfeld (Textfarbe, Hintergrund, beide Verlaufsfarben) hat neben der Farbfläche ein Textfeld für `#1e3a5f`, `#fff` oder `#1e3a5f80`, mit oder ohne `#`; übernommen wird nur ein gültiger Wert, ein halb getippter ist rot markiert, beim Verlassen zeigt das Feld wieder die geltende Farbe. Ältere Werte in anderer CSS-Schreibweise bleiben gespeichert, der Farbwähler zeigt für sie Schwarz. **„Neue Slide"** als gestrichelte Kachel unter der letzten Slide (der Kopf der Liste zeigt nur noch die Anzahl). **Bausteine** als eigene Leiste direkt über der Bühne, mit Symbol und Namen, das Raster daneben. **Kopfleiste des Editors neu:** links ein deutlicher Knopf „‹ Screens" zum Verlassen, Titel und Status; rechts nur Rückgängig/Wiederholen (als Symbole), Player und Speichern; auf dem Handy Titel und Aktionen in zwei Zeilen. **Mit dem Finger:** doppelt so große Griffe auf Touch-Geräten; nur Blöcke und Griffe sperren das Scrollen, auf der leeren Bühne scrollt die Seite. **Offen:** der Inspektor als ausklappbares Blatt am Handy (heute steht er unter der Bühne); Ziehen mit dem Finger ist nur über die Pointer-Events belegt, die auch die Maus nutzt – ein Test auf einem echten Telefon steht aus.
12. ~~**Release-Workflow**~~ **Gebaut am 2026-09-25:** Ein Tag `vX.Y.Z` startet `.github/workflows/release.yml`: zuerst `scripts/release-check.js` (Tag, `package.json`, `package-lock.json` und ein datierter Abschnitt in `CHANGELOG.md` müssen dieselbe Version nennen, sonst Abbruch vor dem Bauen), dann Lint, Typecheck, Tests, Build mit Dist-Prüfung; das ZIP `churchtools-infoscreen-vX.Y.Z.zip` wird GitHub-Release, der CHANGELOG-Abschnitt seine Beschreibung. Ungetaggte Pakete tragen den Commit im Namen. Die Einstellungsseite zeigt die installierte Fassung (`0.1.0` oder `0.1.0+commit`). Die Schritte stehen in `LocalTests.md`. An einer Wegwerf-Kopie durchgespielt; **auf GitHub noch nie gelaufen** – das erste echte Tag ist der Test. **Das Repository ist privat (Stand 2026-09-25):** Releases sehen nur Konten mit Zugriff; für andere Gemeinden müsste es öffentlich werden oder das ZIP anders verteilt werden – eine Entscheidung, keine Technik.
13. ~~**Einrichtungsdoku**~~ **Geschrieben am 2026-09-25:** `docs/Einrichtung.md` für ChurchTools-Administratoren anderer Gemeinden – Installation, das erste Modulrecht für den Administrator selbst (G33), erster Start, Assistent, Gestalter, Geräte-Benutzer mit Benutzernamen (G21) und sparsamem Personenstatus, Fernseher mit Weg A und Kiosk-Einstellungen, Updates, Notbremse (G18), Datenschutz (G14, G29) und eine Fehlertabelle mit den wörtlichen Meldungen. Das README ist für Außenstehende neu geschrieben und verweist darauf. **Nicht am Bildschirm nachgeprüft:** die Beschriftungen der ChurchTools-Oberfläche beim Benutzernamen des Geräte-Kontos und ob die Datenrechte vor dem ersten Start schon „alle" anbieten; G32 steht als offener Hinweis drin.
14. ~~**Freischaltung vorbereiten**~~ **Entfallen am 2026-09-25:** Die Freischaltung kam am 2026-09-24, B6 ist erledigt, der Typ-Snapshot ist nicht möglich (G31), G9 ist beantwortet.
15. ~~**Screen-Einstellungen außerhalb des Editors**~~ **Gebaut am 2026-09-25:** Schema 1.2 mit dem Dokument `schedule` je Screen in der Kategorie `playlists` (Standard-Playlist, Regeln, eigene Revision); beim Lesen überlagert es die Werte im Screen (`withSchedule`), sodass Player, Zeitplan-Auswertung und Kacheln unverändert bleiben; ohne das Dokument gilt der Screen, ein alter Player überspringt es. Der Editor speichert mit `saveContent` nur Slides, Playlists und den Zeitplan, gegen dessen Revision; der Player erkennt Änderungen an Screen- **und** Zeitplan-Revision. Administratoren stellen Name und Overscan im neuen Dialog „…" → „Einstellungen" der Kachel ein (`saveScreenSettings`, gegen die Screen-Revision); „+ Screen erstellen", „Einstellungen" und „Löschen" sehen nur sie; der Inspektor zeigt den Screen nur noch an. Der Assistent gibt Gestaltern Schreibrechte nur noch auf Playlists, Slides und Medien; **„Rechte aktualisieren" nimmt ein vorhandenes Schreibrecht auf Screens und Einstellungen zurück** – per `DELETE /permissions/group_role/{id}` mit `authId` und einer `dataId`, nur was die Rolle tatsächlich hält. **Gemessen am 2026-09-25 (G34):** Das `DELETE` wirkt – nach dem ersten Klick auf „Rechte aktualisieren" schreiben die Designer-Rollen nur noch Playlists, Slides und Medien. **Die Screen-Knöpfe folgen dem Modulrecht** (`create/edit/delete custom data` auf `screens`), nicht „Personen administrieren": Ein ChurchTools-Admin ohne dieses Recht sähe sie sonst und bekäme beim Speichern `403`. Die Einstellungsseite (Rechte vergeben) bleibt an „Personen administrieren" gebunden; im Demo-Modus ohne Modulrechte entscheidet ersatzweise dieses. Die Einstellungsseite warnt, wenn eine Gestalter-Rolle noch Screens schreiben darf. Ursprüngliche Beschreibung: **Screen-Einstellungen außerhalb des Editors** *(gewünscht am 2026-09-24)*: Die Adresse für den Fernseher gehört **nicht in den Editor** – der Editor ist fürs Gestalten der Slides. Eigener Ort je Screen, erreichbar von der Kachel (etwa „…" → „Einstellungen" oder eine eigene Seite): Name, Format, Overscan-Korrektur, später Status. **Der Zeitplan gehört nicht hierher**, sondern zu den Gestaltern (Punkt 17). **Die Adresse ist seit dem 2026-09-25 erledigt:** Die dauerhafte (Weg B) erzeugt ein Administrator in den Einstellungen, der Inspektor zeigt keine mehr; es bleiben Name und Overscan. **Zuständig sind die Administratoren (F, entschieden am 2026-09-25).** Dazu gehört ein Umbau: (1) Das Speichern des Editors schreibt nur noch Slides, Playlists und den Zeitplan – nicht mehr den Index-Wert des Screens; die Konflikterkennung hängt heute an dessen Revision und muss umziehen. **Dafür zieht der Zeitplan aus dem Screen aus** (Schema 1.2): ein eigenes Dokument je Screen, etwa `kind: 'schedule'` in der Kategorie `playlists`, mit eigener Revision; beim Lesen wird ein alter `screen.schedule` übernommen, damit laufende Geräte nichts verlieren. (2) Der Assistent gibt Gestaltern auf `screens` nur noch Lesen, „Rechte aktualisieren" nimmt das Schreiben wieder weg (bisher entzieht er nichts – das ist neu und muss gemessen werden: `DELETE` oder `PUT` ohne `dataId`?). (3) Startseite: „+ Screen erstellen", „Löschen" und die Screen-Einstellungen nur für Administratoren; Gestalter sehen die Kacheln und öffnen den Editor. (4) Die Sperre sind die Rechte, die Oberfläche folgt ihnen – ein Gestalter, der die Adresse kennt, scheitert am `403`.
16. **Mediathek auf der Startseite, der Wiki-Bereich im Hintergrund** *(Idee vom 2026-09-25)* – **der eigene Bereich ist gebaut am 2026-09-25:** Seite `/mediathek`, in der Seitenleiste unter den Filtern für alle Gestalter; dieselbe Liste wie der Dialog im Editor (`useMediaLibrary` und `MediaGrid`), dort zum Auswählen, hier zum Verwalten; Uploads landen auf der Wiki-Seite „Mediathek" (aus dem Editor anfangs auf der Seite des Screens, seit Punkt 19 ebenfalls dort). **Verbergen des Wiki-Bereichs gemessen am 2026-09-25 (G36):** Ohne „Im Menü anzeigen" verschwindet er nicht, er rutscht im Wiki unter eine zuklappbare Gruppe „Ausgeblendet" – aus dem Blick, aber erreichbar. Neu angelegte Bereiche entstehen so; für bestehende gibt es in den Einstellungen die Karte **„Mediathek im Wiki"** mit „Unter ‚Ausgeblendet' führen" / „Wieder unter ‚Kategorien' zeigen" (nur Administratoren, nicht im Demo-Modus). **Entschieden am 2026-09-25: Die Mediathek bleibt flach** – keine Ordner je Screen oder Playlist, weil Bilder geteilt werden (ein Plakat in mehreren Screens und Playlists) und feste Ordner mit jedem Umbau veralten. Ein Bild wird hochgeladen und beliebig oft verwendet; die Ordnung kommt aus Punkt 18. Ursprüngliche Beschreibung: Die Mediathek wird ein eigener Bereich des Designers – in der Seitenleiste unter „Screens", als Seite mit allen Bildern als Kacheln, Hochladen, Löschen mit Nennung der Verwendungen; den Dialog aus dem Editor gibt es schon, er wird dieselbe Liste. Der Wiki-Bereich „Infoscreen" bleibt die **Ablage** – Uploads gehen nur dorthin (G8), Gestalter brauchen dort Bearbeiten-Rechte. **Ob er sich im Wiki verbergen lässt, ist offen (G36):** ChurchTools hat je Bereich „Im Menü anzeigen" (`inMenu`), aber ob das für Leute mit Rechten wirkt, ist laut Forum unklar. Wirkt es, legt der Assistent den Bereich mit `inMenu: false` an und stellt bestehende auf Wunsch um. Wirkt es nicht, bekommt der Bereich einen sprechenden Namen und einen Hinweis, dass die Bilder im Infoscreen Designer gepflegt werden. So oder so: Verbergen ist Ordnung, kein Schutz (G14). Löscht jemand trotzdem ein Bild im Wiki, zeigt der Player einen Platzhalter – das bleibt.
17. ~~**Zeitplan-Oberfläche für die Gestalter**~~ **Gebaut am 2026-09-25, am selben Tag zweimal umgebaut** – erst aus dem Editor auf die Startseite verlegt (der Editor ist fürs Gestalten der Slides), dann mit Punkt 19 auf die Wahl des Screens beschränkt. **Heute:** Auf jeder Screen-Kachel **„Zeitplan"** bzw. **„2 Regeln"** und im Menü „…" → „Zeitplan", für alle Gestalter. Der Dialog wählt die **Standard-Playlist** und die **Regeln** nach Uhrzeit (Wochentage, von–bis) und nach Termin (Minuten vor Beginn bis nach Ende, Kalender) in Vorrang-Reihenfolge mit ↑/↓ und der Nummer als Rang; zur Wahl stehen **alle Playlists im Format des Screens**. „Diese Playlists laufen hier" zeigt sie farbig mit **„Slides bearbeiten"** (speichert Offenes, öffnet den Editor der Playlist); eine neue Playlist lässt sich direkt anlegen. **Vorschau:** Tag wählen (die nächsten 90 Tage, so weit reichen die geladenen Termine), eine farbige Tagesleiste zeigt je Viertelstunde, welche Playlist läuft, ein Regler nennt Uhrzeit, Playlist und entscheidende Regel – mit derselben Funktion wie der Player (`matchingRuleIndex`). Der Dialog lädt und speichert selbst (`saveSchedule`, nur das Zeitplan-Dokument, gegen dessen Revision; bei einem Konflikt „Neu laden" oder „Meine Fassung speichern"). **Gespeichert wird nur ein Zeitplan, der tragen kann** – ohne Wochentag, mit „bis" vor „von" (über Mitternacht: zwei Regeln), ohne Kalender oder mit einer Playlist anderen Formats nennt der Dialog die Regel und speichert nicht; das Repository prüft das Format noch einmal. **Entfallen mit Punkt 19:** Playlists im Dialog anlegen, umbenennen, entfernen; das Verknüpfen einzelner Slides zwischen Playlists; die Auswahl der Playlist im Editor; `playlistIds` im Zeitplan (Schema 1.3, wird überlesen). **Verständlicher gemacht am 2026-09-25** *(Rückmeldung des Nutzers: eine neu angelegte Playlist war im Dialog nicht zu finden, „Diese Playlists laufen hier" las sich wie eine Liste zum Hinzufügen)*: Der Dialog liest sich als Sätze – **„Normalerweise zeigt dieser Screen [Playlist]"**, **„Zu bestimmten Zeiten etwas anderes zeigen"** mit „+ Zu bestimmten Uhrzeiten" / „+ Rund um Termine", jede Regel endet mit „→ zeigt [Playlist]", dann **„Vorschau: Was läuft wann?"** mit einer Farblegende. **Jede Auswahl hat als letzten Eintrag „＋ Neue Playlist anlegen …"**: ein Namensfeld, die neue Playlist wird sofort gewählt (`PlaylistPicker`); legt man eine Regel an, ohne dass es eine zweite Playlist gibt, öffnet sich das Feld von selbst. Neben jeder Auswahl „Slides bearbeiten". Die Liste „Diese Playlists laufen hier" und das freie Anlege-Formular sind entfallen. **Behoben am 2026-09-25** *(gemeldet vom Nutzer: nach dem Neuladen kurz die falsche Playlist)*: Bis die Uhr gegen den Server geprüft ist, läuft absichtlich nur die Standard-Playlist – der Player zeigte sie deshalb für einen Augenblick und schaltete dann um. Jetzt wartet ein Screen **mit Regeln** beim Start ruhig schwarz auf die erste Uhrprüfung (auch mit dem letzten Stand aus dem Gerätespeicher); ohne Regeln erscheint er sofort wie bisher. Scheitert die erste Prüfung am Netz, zeigt er den letzten Stand mit der Standard-Playlist. **Seit 2026-09-25 zeigt die Kachel, was gerade läuft:** Bild und Name der Playlist, die der Zeitplan jetzt zeigt (mit „jetzt", wenn eine Regel entscheidet), ausgewertet wie im Player (`designer/running.ts`, dieselbe Funktion wie auf „Zeitpläne"); ein Klick öffnet diese Playlist. Ursprüngliche Beschreibung: **Zeitplan-Oberfläche für die Gestalter** *(vorgezogen aus „Später" am 2026-09-25 – Anwendungsfall: Screens zeigen je nach Uhrzeit anderes)*: Das Datenmodell und der Player können es seit Phase 1 (Abschnitt „Playlists und Zeitpläne"): mehrere Playlists je Screen, eine Standard-Playlist, Regeln **nach Uhrzeit** (Wochentage, von–bis) und **nach Termin** (Minuten vor und nach jedem Termin ausgewählter Kalender – verschiebt sich mit dem Termin). Es fehlt die Oberfläche: im Editor Playlists anlegen, benennen und wechseln (die Slide-Liste zeigt die gewählte Playlist); Regeln anlegen und ordnen, mit der Vorrangregel sichtbar; eine **Vorschau „was liefe Sonntag 10:30?"** mit Zeitregler. **Wer:** die Gestalter (F) – deshalb zieht der Zeitplan vorher aus dem Screen-Index aus (Punkt 15, Schema 1.2). Termin-Regeln brauchen Kalenderrechte fürs Gerät; „Rechte aktualisieren" deckt sie schon ab (`calendarIdsInUse` liest den Zeitplan mit). Später auf der Kachel: welche Playlist gerade läuft.
18. ~~**Mediathek: „Verwendet in", Filter, „Unbenutzt"**~~ **Gebaut am 2026-09-25** *(entschieden am selben Tag statt Ordnern)*: Unter jedem Bild stehen bis zu zwei Orte, der Rest als „und N weitere" mit allen im Tooltip; ein Bild ohne Verwendung heißt „Unbenutzt". `ScreenRepository.mediaUses()` liest Screens, Playlists und Slides **einmal** für die ganze Mediathek; eine Playlist, die kein Screen zeigt, erscheint ohne Screen („Gottesdienst › Begrüßung"). **Filter entschieden am 2026-09-25:** Die Suche findet ein Bild auch über Screen, Playlist und Slide, dazu die Chips „Alle | Verwendet | Unbenutzt" – statt eigener Auswahllisten für Screen und Playlist, die bei wenigen Screens mehr Fläche als Nutzen hätten. Ursprüngliche Beschreibung: unter jedem Bild, wo es gezeigt wird – „Foyer › Gottesdienst › Begrüßung", abgeleitet aus Slides, Playlists und Zeitplänen und deshalb immer richtig – statt der Wiki-Seite, auf der es hochgeladen wurde (die bleibt Ablage im Hintergrund). Filter nach Screen und Playlist und **„Unbenutzt"** zum Aufräumen. Die Verwendungen in einem Durchgang ermitteln, nicht je Bild (heute liest `mediaUsage` alles je Aufruf). **Danach, optional:** frei vergebene Schlagworte am Medium („Jugend", „Saisonal", „Logo") für Bilder, die noch nirgends verwendet sind – im Schema als optionales Feld, schließen sich anders als Ordner nicht aus.
19. ~~**Playlists für sich, zwischen Screens geteilt**~~ **Gebaut am 2026-09-25** *(Entscheidung des Nutzers am selben Tag: „Playlists sollen zwischen Screens geteilt werden können … unabhängig vom Screen erstellt werden. Der Screen wählt dann die entsprechende Playlist")*: **Schema 1.4** – eine Playlist trägt **Format** (`stage`), **Revision** und `updatedBy`; ältere bekommen Format und – hießen sie „Standard" und zeigt sie genau ein Screen – dessen Namen beim Lesen (`withPlaylistDefaults`), gespeichert wird das erst mit dem nächsten Speichern; nichts wird ungefragt umgeschrieben. **Seite „Playlists"** in der Seitenleiste unter den Screen-Filtern: Kacheln mit erster Slide, Format, Anzahl Slides und **„läuft auf: Foyer, Café"** bzw. „auf keinem Screen"; Suche nach Playlist oder Screen, Filter nach Format; **„+ Playlist erstellen"** (Name, Format; beginnt mit einer leeren Slide); **Löschen** nur, solange kein Screen sie zeigt (`PlaylistInUseError`). **Der Editor hängt an der Playlist** (`/playlists/:id`): Titel ist ihr Name, im Inspektor Name, Format und „Läuft auf"; gespeichert wird nur die Playlist mit ihren Slides, gegen ihre eigene Revision (`savePlaylist`) – zwei Gestalter an verschiedenen Playlists desselben Screens stören sich nicht mehr. „Player" öffnet einen Screen, der sie zeigt. Die **Screen-Kachel** öffnet weiter mit einem Klick den Editor – den der Standard-Playlist, deren Name auf der Kachel steht; „Zurück" führt dorthin, woher man kam. Ein **neuer Screen** bekommt eine eigene Playlist mit seinem Namen. **Aufräumen:** Playlists werden nie mehr von selbst gelöscht, nur Slides ohne Playlist und Zeitpläne gelöschter Screens. **Player:** erkennt Änderungen jetzt auch an den Revisionen der gezeigten Playlists (`configVersion`). **Mediathek:** Uploads gehen alle auf die Wiki-Seite „Mediathek" – es gibt keinen Screen mehr, nach dem eine Seite hieße; „wird verwendet" nennt „Playlist › Slide". **Offen:** Slides aus einer anderen Playlist übernehmen (kopieren oder verknüpfen – verknüpfte Slides bräuchten eine eigene Revision); eine Playlist duplizieren; alte Links `/screens/<slug>` führen ins Leere (nie veröffentlicht).
20. ~~**Hervorgehobene und kommende Events mit Bild**~~ **Gebaut am 2026-09-25, als Optik des WordPress-Plugins** *(Klärung des Nutzers: „hervorgehoben" meint den Baustein „Nächster Termin", der das Bild schon zeigt; Design aus dem eigenen WP-Plugin `churchtools-plugin` übernehmen – Listenansicht und hervorgehobenes Event; übernommen ist der Aufbau, nicht der Code)*: **Schema 1.8** – `layout` an Terminliste (`rows` | `cards`) und Nächster Termin (`classic` | `card`), im Inspektor „Darstellung". **Karten:** Datumskachel (Tag groß, Monat klein, in der Kalenderfarbe getönt), Kalender-Etikett in der Kalenderfarbe (Schrift schwarz oder weiß nach Helligkeit), Titel, darunter Wochentag mit Uhrzeit von–bis und Ort – zwei feste Zeilen, damit das seitenweise Blättern richtig rechnet. **Hervorgehoben:** Karte mit Kachel, Etikett, großem Titel, Untertitel, Beschreibung (höchstens drei Zeilen, ohne Markup), Datum, Uhrzeit, Ort, das Terminbild daneben. **Neu gelesen:** Ort aus `base.address` (Name und Zusatz, „Gemeindezentrum, Saal" – wie das Plugin; Straße und Ort sagen im eigenen Foyer wenig) und die Beschreibung. **Anders als im Plugin:** Farben aus der Kalenderfarbe mit Transparenz statt heller Pastelltöne – der Fernseher hat meist einen dunklen Hintergrund; keine Monatsüberschriften (entschieden bei Punkt 23). Ältere Player zeigen die schlichte Form. Ursprüngliche Idee *(vom 2026-09-25)*: Termine aus ChurchTools **samt ihrem Bild** zeigen – die als hervorgehoben markierten („Highlights") und die nächsten kommenden. **Vorab in der Academy klären,** wie ChurchTools „hervorgehoben" nennt und wo es steht (Kalender-Termin, Event, öffentlicher Kalender?) und ob die API es liefert. **Vorhanden:** Termine tragen ihr Bild schon (`imageUrl`, Bilddienst mit `w` und `h`, G14), der Baustein „Nächster Termin" zeigt es (`showImage`). **Fehlt:** ein Baustein, der mehrere Termine mit Bild zeigt (Kacheln oder nacheinander), ein Filter „nur hervorgehobene", und eine Antwort, was ohne Bild erscheint. Zu prüfen: ob das Gerät die Bilder ohne Anmeldung laden darf (wie die Mediathek, G14) und ob Bilder privater Termine so nach außen gelangen. **Nachgezogen am 2026-09-25** *(Nutzer: das Zwischendesign – Kategorie und Uhrzeit in einem Block links – „gefällt mir nicht. Orientiere dich bitte am Wordpress Plugin. Bei Nächster Termin sollen die Infos Wochentag+Datum und Uhrzeit untereinander stehen")*: Die Karte der Terminliste folgt jetzt der Listenansicht des Plugins (`docs/screenshots/liste.png` dort): Datumskachel links, mittig zur Höhe; rechts Kalender-Etikett, Titel, Untertitel und darunter **Wochentag mit Datum, Uhrzeit, Ort je in einer eigenen Zeile**; Trennlinie zwischen den Einträgen. Beim „Nächsten Termin" stehen dieselben Angaben ebenfalls untereinander. Die Beschreibung bleibt in der Liste weg – sie machte jeden Eintrag auf dem Fernseher doppelt so hoch. **Folge fürs Blättern (23):** Die Einträge sind unterschiedlich hoch (mit oder ohne Untertitel und Ort). Eine unsichtbare Kopie der Liste misst jeden Eintrag, die Seiten werden nach diesen Höhen gefüllt (`paginateByHeight`, Platz für den Seitenbalken nur bei mehreren Seiten); ohne „Alle Termine" zeigt die Liste nur Einträge, die ganz in die Box passen. **Und noch einmal am selben Tag** *(Nutzer: „ziehe den Titel nur in der Listenansicht nach rechts. Dann ist in der Reihe: Datumschip, dann Datum über Uhrzeit, Titel, Kategorie am Ende (Kategorie alleinig rechtsbündig)")*: In der Liste steht jeder Eintrag jetzt in einer Reihe – Kachel, eine gleich breite Spalte mit Wochentag und Datum über der Uhrzeit (damit alle Titel auf einer Linie beginnen), Titel mit Untertitel und Ort darunter, das Kalender-Etikett rechtsbündig. „Nächster Termin" bleibt, wie er ist.
21. ~~**Zeitpläne in der Seitenleiste**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag)*: Seite `/zeitplaene`, in der Seitenleiste über „Playlists" (Reihenfolge: Screens, Zeitpläne, Playlists, Mediathek – vom Gerät zum Inhalt). Je Screen eine Zeile: Name, Format, **„Jetzt: …"** (ausgewertet mit `matchingRuleIndex` und den Terminen der Regel-Kalender, wie im Player) und darunter die Regeln in Worten – „1 So 09:00–12:00 → Gottesdienst", „30 Min. vor bis 15 Min. nach Terminen in Gottesdienste → …", zuletzt „sonst → Wochenüberblick (Standard)"; die gerade entscheidende Zeile ist markiert. „Bearbeiten" öffnet denselben Dialog wie die Kachel. **Vorschau seit 2026-09-25:** links neben den Regeln die erste Slide der Playlist, die gerade läuft; ein Klick auf eine Regelzeile zeigt deren Playlist, ein Klick aufs Bild öffnet sie im Editor. **Am Handy** steht die Zeile aus Filtern und Bereichen jetzt auf jeder Seite des Moduls, nicht nur auf der Startseite – sonst käme man von „Playlists" nicht zu „Zeitpläne".
22. ~~**Termin-Regeln mit eigenem Zeitraum**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag: vor dem Gottesdienst eine Playlist, die Gäste begrüßt, danach eine, die verabschiedet – und weil der Gottesdienst unterschiedlich lange dauert, nach einer festen Zeit ab Beginn umschalten)*: **Schema 1.5** – eine Termin-Regel hat `from` und `to`, je ein Anker (Beginn oder Ende des Termins) mit Minuten davor oder danach; der Zeitraum gilt von `from` bis ausschließlich `to`. Beispiele: Begrüßung „30 Min. vor Beginn bis 10 Min. nach Beginn", Verabschiedung „75 Min. nach Beginn bis 105 Min. nach Beginn" (feste Zeit ab Beginn, unabhängig vom eingetragenen Ende) oder „Ende bis 30 Min. nach Ende". **Im Dialog:** Vorlagen „Vor Beginn", „Während", „Nach dem Ende", „Rundherum" (Voreinstellung wie bisher: 30 Min. vor Beginn bis 15 Min. nach Ende), darunter „von [30] Min. [vor] [Beginn] bis [10] Min. [nach] [Beginn]". Ein Zeitraum, der nie aufgehen kann („bis" vor „von" am selben Anker, oder von einem Ende-Punkt bis zu einem Beginn-Punkt), wird genannt und nicht gespeichert. **Ältere Player:** Regeln ohne `from`/`to` gelten wie bisher (vor Beginn bis nach Ende); `minutesBefore`/`minutesAfter` werden weiter mit dem nächstliegenden alten Zeitraum gefüllt, damit ein Player vor 1.5 bis zu seinem nächtlichen Neuladen etwas Sinnvolles zeigt – die Begrüßung dort also bis zum Ende des Gottesdienstes statt bis 10 Min. nach Beginn. **Nicht geprüft:** ein Zeitraum, der über Mitternacht in den nächsten Tag reicht, wenn der Termin am Vortag begann (der Player holt Termine ab Beginn des heutigen Tages).
23. ~~**Terminliste: alle Termine der nächsten X Tage, seitenweise**~~ **Gebaut am 2026-09-25** *(Entscheidungen des Nutzers am selben Tag: die Slide verlängert sich, wenn ihre Dauer nicht reicht; 10 s je Seite; keine Tagesüberschriften)*: **Schema 1.6** – `showAll` und `pageSeconds` (3–120, Vorgabe 10) an der Terminliste; höchstens 200 Termine. Der Block misst eine Zeile (`offsetHeight`), teilt die Blockhöhe und hält bei mehreren Seiten Platz für die Seitenzahl („2/3" unten rechts, dezent); die Seiten blenden über (nur Deckkraft). **Zeit:** je Seite mindestens `pageSeconds`; ist die Slide länger, teilen sich die Seiten ihre Dauer (60 s, 3 Seiten → je 20 s); reicht sie nicht, bleibt die Slide Seiten × Sekunden – der Block meldet seine Seitenzahl in den Bühnen-Kontext, die Rotation prüft beim Ablauf nach (`slideSeconds`). Eine Seite allein verlängert nichts. **Im Editor:** „Alle Termine zeigen, seitenweise" ersetzt „Höchstens" durch „Sekunden je Seite", darunter „Ergibt 3 Seiten à 10 s – die Slide läuft dafür 30 s statt 8 s"; die Vorschau hält Seite 1. Ältere Player zeigen die ersten „höchstens N". **Nachgebessert am selben Tag** *(Rückmeldung des Nutzers: die Liste blieb auf „1/2")*: Ein Beobachter lieferte bei jedem Aufruf ein neues Array; weil die Uhr des Players jede Sekunde tickt, galt das jede Sekunde als Änderung und setzte die Liste auf Seite 1 zurück – jetzt einzelne Quellen, die nach Wert verglichen werden, und der Render-Test lässt die Uhr mitlaufen. Dazu **ein Balken unter den Terminen**, der sich je Seite über deren Zeit füllt (nur `transform`, auf eigener Ebene), neben der Seitenzahl; im Editor, wo die Liste Seite 1 hält, und bei angehaltener Vorschau fehlt er. **Sichtbar gemacht am selben Tag** *(Frage des Nutzers: überschreibt die Seitenzeit die Slide-Dauer?)*: Ja – die eingestellte Dauer bleibt gespeichert, gespielt wird die längere. Die Slide-Liste zeigt dann „12 → 30 s" (mit Erklärung beim Darüberfahren), der Inspektor unter „Anzeigedauer" „Läuft 30 s – so lange braucht die Terminliste für alle Seiten". **Nicht gebaut:** Blättern in der Vorschau per Klick. Ursprüngliche Idee *(vom 2026-09-25)*: Heute zeigt die Terminliste die ersten „höchstens N" Termine im Zeitraum „Tage voraus"; was nicht in die Box passt, wird abgeschnitten. Neu: eine Option **„Alle Termine zeigen"** – passen sie nicht in die Box, **blättert die Liste seitenweise durch** (Seite 1/3 …, sanft überblendet). **Vorschlag zum Bau:** (1) *Wie viele passen:* Jede Zeile ist eine Textzeile (`nowrap`), der Player misst die Höhe einer Zeile im Block (`offsetHeight`, unabhängig von der Skalierung der Bühne) und teilt die Blockhöhe – neu messen, wenn Schrift, Größe oder Blockhöhe sich ändern und sobald die Schriften geladen sind. (2) *Zeitlich:* **Die Slide bleibt, bis alle Seiten gezeigt sind** – je Seite eine einstellbare Zeit (Vorgabe: die Anzeigedauer der Slide), die Slide dauert dann Seiten × Seitenzeit; der Block meldet seine Seitenzahl an die Rotation. Alternative: feste Seitenzeit innerhalb der Slide-Dauer, weiter beim nächsten Durchlauf – kürzer, aber es fehlen Termine, wenn die Slide zu kurz ist. (3) *Anzeige:* Seitenzahl dezent unten rechts („2/3") oder als Punkte; abschaltbar. (4) *Editor:* Die Vorschau zeigt Seite 1 und im Inspektor „ergibt 3 Seiten"; Blättern in der Vorschau per Klick. (5) *Schema:* optionale Felder am Block (`showAll`, `pageSeconds`, Minor-Version); `limit` bleibt gefüllt, damit ältere Player die ersten N zeigen. Beide Fragen sind mit den Entscheidungen oben beantwortet: Die Slide verlängert sich, Tagesüberschriften gibt es nicht.
24. ~~**Vorschau im Editor**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag beim Testen: Änderungen sehen, ohne die Playlist zu speichern)*: Knopf **„Vorschau"** oben im Editor spielt die Playlist **aus dem ungespeicherten Entwurf** im Vollbild ab – mit derselben Rotation wie der Fernseher (aus dem Player herausgezogen nach `src/player/rotation.ts`, beide nutzen sie), blätternden Terminlisten, Überblendung und Live-Terminen der Editor-Vorschau; abgeschaltete Slides bleiben wie auf dem Fernseher weg. Beginnt bei der gewählten Slide. Unten eine Leiste „Vorschau – nicht gespeichert", vor/zurück, Anhalten (hält auch das Blättern), „2/3 · Termine · 30 s", Schließen; Tasten ←/→, Leertaste, Esc. Die Leiste und der Mauszeiger verschwinden nach 2,5 s ohne Bewegung. Die Tasten des Editors (Entf, Pfeile) wirken dahinter nicht. Gespeichert wird nichts. **Offen, falls gewünscht:** die Vorschau in einem zweiten Fenster, das beim Bearbeiten live mitläuft (etwa über `BroadcastChannel`) – für zwei Bildschirme.
25. ~~**Bausteine sperren (Schloss)**~~ **Gebaut am 2026-09-25** *(Entscheidung des Nutzers: „Sperren soll einfach das gesamte Element sperren")*: **Schema 1.7** – `locked` am Baustein, nur für den Editor. Im Inspektor oben „Sperren" / „Gesperrt"; gesperrt sind alle Felder und Knöpfe des Bausteins aus (ein `fieldset disabled`), er lässt sich nicht ziehen, skalieren, per Pfeil- oder Entf-Taste ändern, nicht in der Ebene verschieben und nicht löschen – der Store verweigert jede Änderung, egal woher sie kommt. Anklicken geht weiter (sonst ließe er sich nicht entsperren); am Rahmen dann ein Schloss statt der Griffe. Sperren und Entsperren sind je ein Rückgängig-Schritt. **Durchgreifen seit 2026-09-25:** Ein Klick auf einen gesperrten Baustein wählt den obersten nicht gesperrten, der an dieser Stelle darunter liegt (`blockBelow`), und lässt ihn gleich ziehen; liegt dort keiner, wird der gesperrte gewählt. Mit Alt (Mac: Option) nimmt der Klick immer den angeklickten. Der Tooltip am Rahmen sagt es. Ursprüngliche Idee *(vom 2026-09-25)*: Jeder Baustein bekommt ein Schloss. Gesperrt lässt er sich auf der Bühne nicht mehr verschieben, skalieren oder versehentlich löschen – etwa Gemeindekopf, Uhr oder ein Hintergrundbild, die auf jeder Slide gleich stehen sollen. **Vorschlag:** ein Feld `locked` am Baustein (optional, Minor-Version; ältere Player ignorieren es, es betrifft nur den Editor); Schloss-Knopf im Inspektor und als kleines Symbol am Rahmen; gesperrt: kein Ziehen, keine Griffe, Entf/Pfeiltasten wirken nicht, Klick wählt ihn trotzdem aus (damit man ihn im Inspektor entsperren oder Text und Farbe ändern kann – **offen:** ob gesperrt auch Inhalt und Stil sperrt oder nur Lage und Größe). Ein Klick auf einen gesperrten Baustein, der unter einem anderen liegt, sollte zum darüberliegenden durchgreifen können, sonst blockiert ein gesperrtes Hintergrundbild die Auswahl.
26. ~~**Schnellcheck: Änderungen in etwa 20 Sekunden auf dem Fernseher**~~ **Gebaut am 2026-09-25** *(Frage des Nutzers: kann das Speichern die Screens zum Neuladen anstoßen?)*: Anstoßen geht nicht – ChurchTools bietet einem Modul keinen bekannten Weg, Nachrichten an offene Browser zu schicken (nicht gemessen), und Geräte lesen nur. Stattdessen sieht der Player **alle 20 Sekunden** (±20 %) nach, **mit einer einzigen Anfrage auf die Kategorie `playlists`** (`contentRevisions`): Dort hinterlässt jedes Speichern eines Gestalters eine neue Revision – der Playlist oder des Zeitplans. Weicht eine davon vom geladenen Stand ab, lädt er sofort den ganzen Screen (`scheduleConfig(0)`) und übernimmt ihn wie bisher ohne Neuladen der Seite, auf der gezeigten Slide. Der 2-Minuten-Abruf bleibt für die Änderungen der Administratoren (Name, Overscan) und als Rückfall; scheitert der Schnellcheck, meldet er nichts selbst und wartet bis zum nächsten 2-Minuten-Takt. Last je Fernseher: etwa 3 statt 2 Anfragen pro Minute (G16: kein Limit in Reichweite). Im Editor steht nach dem Speichern „– die Fernseher zeigen es in etwa 20 s" (nicht im Demo-Modus, dort übernimmt ein offener Player sofort).
27. ~~**Design-Backend**~~ **Gebaut am 2026-09-25** *(Antworten des Nutzers: Ecken, Primärfarben, Schalter „Nativ"/„Groß"; das Bild 16:9 „bzw. Format auch im Design Backend wählbar")*: **Schema 1.9** – ein Dokument `kind: 'theme'`, `id: 'theme'`, in der Kategorie `playlists` (Gestalter schreiben, Geräte lesen – keine neuen Rechte), gespeichert gegen seine Revision wie eine Playlist. Eigene Seite **„Design"** in der Seitenleiste mit Live-Vorschau aus echten Terminen. Felder: **Ecken** rund/eckig (Kacheln, Etiketten, Karten, Bilder, Seitenbalken – als CSS-Variablen `--isd-radius`, `--isd-pill` an der Slide), **Akzent** (Kalender ohne eigene Farbe, Balken), **Text** und **Hintergrund** (nur für neue Slides und Bausteine, Bestehendes bleibt), **Termine** „Nativ" (Zeilen, schlichter nächster Termin) oder „Groß" (Karten, hervorgehoben) – gilt für jeden Terminbaustein ohne eigene Darstellung; im Inspektor heißt das „Wie im Design", **Format der Terminbilder** 16:9 (Vorgabe), 3:2, 4:3, 1:1 oder frei; das Bild wird in genau diesem Kasten vom Bilddienst geholt (`fit=crop`). Der Schnellcheck (26) sieht auch die Revision des Designs: Speichern zeigt sich in etwa 20 s auf allen Fernsehern. **Entschieden ohne Rückfrage** (Frage offen gelassen): *ein* Design für alle Screens, änderbar von allen Gestaltern. Ältere Player (≤ 1.8) überspringen das Dokument mit einer Warnung in der Konsole; ältere Designer lassen dann das Aufräumen verwaister Slides aus, bis sie aktualisiert sind. **Entschieden am 2026-09-25:** Die Schrift folgt ChurchTools – neue Bausteine bekommen Lato wie ChurchTools selbst, ein eigenes Feld im Design gibt es vorerst nicht; je Baustein bleibt jede der zehn Schriften wählbar. Bestehende Slides behalten ihre Farben, es gibt kein „auf alles anwenden" – noch hat niemand Bestand, der umgestellt werden müsste. **Aufbau der Seite seit 2026-09-25:** zwei Boxen nebeneinander – die Einstellungen (Ecken, Farben, Termine, Terminbilder) untereinander, daneben die Vorschau; beide höchstens 640 px breit, denn breiter wurden Felder und Bühne nur größer, nicht deutlicher; schmal steht die Vorschau oben. „Speichern" und „Verwerfen" sitzen oben rechts in der Leiste wie das Anlegen auf den anderen Seiten.
28. ~~**HTML-Block / Webseite einbetten**~~ **Gebaut am 2026-09-25 als „Webseite" und „QR-Code"** *(Nutzer: das bisherige HTML-Schnipsel – Überschrift, QR-Code von `api.qrserver.com`, Instagram-Profil per `<iframe src="https://www.instagram.com/<name>/embed/">` – „gefällt mir so eh nicht mehr, wenn wir das besser hinbekommen sehr gerne")*: Statt eines HTML-Blocks zwei Bausteine, aus denen die Slide mit Text-Bausteinen gebaut wird und die das Design übernehmen. **Webseite** (Schema 1.9, `web`): Adresse, nur `https`; fremde Seiten im Rahmen mit `sandbox="allow-scripts allow-same-origin"`, Seiten der eigenen Instanz nur mit `allow-scripts`; `referrerpolicy="no-referrer"`; nicht bedienbar (`pointer-events: none`); **Größe der Seite** 50–300 % (die Seite wird bei 200 % auf halber Blockgröße gelegt und hochskaliert – für Seiten, die fürs Handy gemacht sind). Ein Instagram-Name oder eine Profiladresse wird zur Einbettungsseite. **Gemessen (2026-09-25, `curl` ohne Anmeldung):** `https://www.instagram.com/<name>/embed/` antwortet mit 200, ohne `X-Frame-Options` und ohne `frame-ancestors` – darf also eingebettet werden. **QR-Code** (`qr`): Inhalt, Farbe, Hintergrund; entsteht auf dem Gerät mit `qrcode-generator` (MIT, ohne Abhängigkeiten), UTF-8 selbst kodiert – kein fremder Dienst, offline lesbar. Offline zeigt der Webseiten-Rahmen nichts. Die Google-Schrift des alten Schnipsels ließe die CSP nicht zu (`style-src`/`font-src`, G15) – Barlow Semi Condensed liegt ohnehin im Modul. **Nicht gebaut:** HTML/CSS als Code (`srcdoc`) – bleibt nach Abschnitt „Eigener Web-Code" beim eigenen Recht. **Nachtrag vom 2026-09-25:** Die Instagram-Einbettung überzeugte den Nutzer im Test nicht (vermutlich schränkt Instagram die Einbettung selbst ein): „Instagram lösen wir in der Zukunft anders." Die Umwandlung eines Profilnamens in die Einbettungsadresse und jeder Hinweis auf Instagram sind deshalb wieder aus dem Designer genommen; der Baustein „Webseite" bleibt als allgemeiner Rahmen für https-Seiten. **Entschieden am 2026-09-25: Socials bleiben draußen.** Instagram automatisch ginge nur noch über ein Profi-Konto, eine eigene Meta-App und einen Zugangsschlüssel, der alle 60 Tage erneuert werden muss – und der im Modul für jedes Gerät lesbar läge; das trägt eine Gemeinde nicht. Ein **YouTube-Baustein** (Video, Livestream über die feste Kanal-Adresse `/embed/live_stream?channel=…`) ist für andere Anwender denkbar, wir selbst bringen den Livestream auf anderem Weg auf die Fernseher; er bräuchte `youtube-nocookie.com` und eine Referrer-Angabe, denn ohne sie verweigert YouTube seit Ende 2025 die Einbettung („Error 153") – im Baustein „Webseite" (`no-referrer`) geht YouTube deshalb nicht. **Stattdessen als Kandidat: Beiträge aus ChurchTools** – öffentliche Beiträge öffentlicher Gruppen sind laut Academy ohne Anmeldung lesbar und fürs Einbetten vorgesehen; zu messen mit einem Testbeitrag, bevor gebaut wird. **Test des Nutzers (2026-09-25):** die eigene Webseite läuft im Rahmen, `google.de` nicht – Google sendet `X-Frame-Options: SAMEORIGIN` (per `curl` nachgesehen), das kann kein Rahmen umgehen. Dazu wurde eine Adresse ohne `https://` bisher gar nicht gezeigt; der Designer ergänzt es jetzt selbst, und der Hinweis im Inspektor nennt große Seiten wie Google als Beispiel für verbotenes Einbetten.
29. ~~**Die Bereiche angleichen**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag, Anlass: Punkt 18)*: Screens, Zeitpläne, Playlists, Mediathek und Design sind aus denselben Teilen gebaut – `PageHeader` (Symbol, Titel, eine Zeile Erklärung in voller Breite statt einer schmalen Spalte), `SearchField`, `GroupCard` (runde Marke, Titel mit Anzahl, Filter-Chips rechts) und `FilterChips`. **Anlegen und Hochladen** sitzen überall oben rechts in der Leiste („+ Screen erstellen", „+ Playlist erstellen", „+ Bilder hochladen"); der Dialog im Editor behält seinen Knopf im Kopf. **Der Rahmen steht sofort:** Bisher zeigten vier Seiten bis zum Ende des Ladens nur „Lade …", ein Wechsel im Menü sah aus wie ein Neuladen des Designers; jetzt lädt jede Seite in ihrem Rahmen, und ob jemand Administrator ist, fragt das Modul einmal je Besuch (`administrator.ts`), nicht je Seite. **Der Hintergrund reicht bis unten:** `min-height: 100%` griff nicht, weil die Hostseite ihren Elementen keine Höhe gibt; der Rahmen misst seinen Abstand nach oben und füllt das Fenster darunter. **Die Einstellungen folgen seit 2026-09-25 ebenfalls:** gleicher Kopf, volle Breite, gleiche Kartenabstände.
30. ~~**Neuigkeiten im Modul**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag als Voraussetzung für `v0.1.0`: „damit man sieht, was neu ist" – Anwenderfokus)*: Seite **„Über & Neuigkeiten"** (`/ueber`) für alle, in der Seitenleiste unter den Bereichen: installierte Version, was das Modul kann, Links zu Versionen, Fehlermeldungen und Quellcode auf GitHub, Lizenz. Darunter **`CHANGELOG.md` selbst**, beim Bauen eingebunden – ein Text für die Release-Notizen auf GitHub und für die Anwender, kein zweiter, der veraltet. Gelesen wird nur, was „Keep a Changelog" nutzt (Versionen, Gruppen, Punkte mit Fett, Code, Links), und als Daten dargestellt, nie als HTML; Verweise auf Dokumente des Repositorys öffnen auf GitHub. Die installierte Version trägt „Installiert", Unveröffentlichtes „In Arbeit". **Ein Punkt in der Seitenleiste** zeigt eine noch nicht angesehene Version an; gemerkt wird das nur im Browser (`localStorage`), ohne Speicher bleibt der Punkt eben stehen. Folge für jedes Release: **Die CHANGELOG-Einträge sind Anwendertext** – was jemand im Designer oder am Fernseher merkt, nicht wie es gebaut ist.
31. ~~**Playlists duplizieren, Slides übernehmen**~~ **Gebaut am 2026-09-25** *(Rest aus Punkt 19 und „Später")*: Im Menü „…" einer Playlist **„Duplizieren"** – eine neue Playlist „… (Kopie)" mit Kopien aller Slides, die gleich im Editor öffnet; sie läuft nirgends, bis ein Zeitplan sie wählt. Im Editor unter „Neue Slide" **„Aus anderer Playlist …"** – ein Dialog mit allen anderen Playlists desselben Formats und ihren Slides als Bilder, Auswahl per Klick, übernommen hinter die aktuelle Slide. **Entschieden: immer Kopien, nie Verweise** – das Schema erlaubt eine Slide in mehreren Playlists, aber eine Änderung, die still in einer anderen Playlist landet, überrascht; wer dieselbe Slide überall will, dupliziert die Playlist. Ein Hochkant-Slide auf einer Querbühne würde abgeschnitten, deshalb nur dasselbe Format.
32. ~~**Countdown und Hinweisband**~~ **Gebaut am 2026-09-25** *(gewünscht am selben Tag, nachdem Socials herausgefallen sind)*: **Schema 1.10.** **Countdown** – ein Baustein, der bis zum Beginn des nächsten Termins gewählter Kalender zählt: „Gottesdienst beginnt in 12:34", ab einer Stunde „1:05:09", ab einem Tag „2 Tage 3 Std.", darunter Tag und Uhrzeit; ganztägige Termine zählen nicht mit, weil sie keine Stunde haben. Während eines Termins steht `runningText` („Läuft gerade"); leer gelassen, zählt er gleich zum nächsten. Er zählt im Sekundentakt selbst weiter, damit die Vorschau im Designer (Takt 30 s) aussieht wie der Fernseher. Passt zu einer Zeitplan-Regel „30 Minuten vor Beginn". **Hinweisband** – kein Baustein, sondern ein Feld `banner` der Playlist: Laufschrift oder stehend, oben oder unten, Höhe, Tempo, Farben, und **„Zeigen bis"** – danach ist es von selbst weg. Es liegt auf der Bühne neben den Slides, deshalb läuft es beim Slidewechsel weiter statt neu anzufangen, und es gilt für alle Slides der Playlist. Die Zeit „bis" ist die Wanduhr der Gemeinde (`YYYY-MM-DDTHH:mm`, wie ein Datumsfeld sie liefert) und wird in deren Zeitzone verglichen, nicht in der des Geräts. Die Laufschrift bewegt sich nur per CSS-Transform (wie die Sanduhr), was ein Pi schafft. Ältere Player überspringen den Countdown und zeigen das Band nicht. Nebenbei: welche Kalender ein Baustein braucht, sagt jetzt eine Funktion (`blockCalendarIds`) statt sieben Stellen.

**Weg B gebaut am 2026-09-25** – Weg A trägt keinen Dauerbetrieb, weil auch „Angemeldet bleiben" nur 24 Stunden hält (G32). ChurchTools nimmt `login_token` in der Adresse beim Laden an und entfernt ihn dabei (G9); deshalb trägt die Adresse den Token **im Query** (Anmeldung bei jedem Laden) **und im Fragment** (übersteht die Weiterleitung, in Chromium und WebKit gemessen). Der Player liest ihn aus dem Fragment, meldet sich bei abgelaufener Sitzung selbst neu an und **lädt sich nur über die Adresse mit Token neu** – das nächtliche Neuladen erneuert die Sitzung also täglich (e2e-Test mit vorgespulter Uhr). Die Adresse erzeugt ein Administrator in den **Einstellungen** (Karte „Adresse für einen Fernseher"): Benutzername und Passwort des Geräte-Kontos → `POST /api/login/token` ohne Cookies → Adresse; Passwort und Token werden nirgends gespeichert, auch nicht in `settings`, die Gestalter lesen können. Die vorläufige Adresse im Inspektor ist entfernt. **Offen:** der Test auf einem echten Fernseher über mehrere Tage.

**Seit der Freischaltung (2026-09-24) erledigt:** Extension unter `infoscreen-designer` hochgeladen (B6), Rechte-Nummern abgelesen (G33), Speichern und Laden über den echten Speicher der Extension (ein Screen im Designer gespeichert, vom Player gelesen), Player im Browser eines Geräte-Benutzers (G35). **Nicht möglich:** der Typ-Snapshot (B5) – die Spezifikation führt keine Modul-Pfade (G31). **Login-Token am `/ccm/`-Pfad (G9): beantwortet am 2026-09-25, trägt.** **Offen:** Fixtures der Modul-Antworten aufzeichnen und vor dem 2026-10-22 sichern; der Test auf einem echten Fernseher mit Kiosk-Browser; ob „Angemeldet bleiben" länger als 24 Stunden hält (G32) – **nein, beantwortet am 2026-09-25**. G10 nur, wenn es nebenbei geht.



**Nicht anfassen, bis ein Produktschritt es braucht:** G16, C4, `attachments`, `ct-events-load`, das Gespräch mit dem Autor von `ct-pass-store`. Die Frage 306/403 aus G21 ist seit G30 beantwortet.

## Quellen

- ChurchTools Extension Boilerplate: <https://github.com/churchtools/extension-boilerplate>
- ChurchTools JS-Client: <https://github.com/churchtools/churchtools-js-client>
- `ct-pass-store` (MIT), läuft auf unserer Produktivinstanz: <https://github.com/lub90/ct-pass-store> – ergiebig sind `extension/src/ct-utils/lib/ExtensionData.ts`, `extension/vite.config.ts`, `extension/scripts/package.js`, `extension/README.md` (Rechte je Rolle)
- Referenz-Extension: <https://github.com/aschojz/churchtools-extension-publisher>
- Extension mit Termin-Persistenz: <https://github.com/bensteUEM/ct-events-load>
- Forum: [Infoscreen mit FullPageOS](https://forum.church.tools/topic/10280), [Wünsche der Anwender](https://forum.church.tools/topic/6252), [Login-Token für den Infoscreen](https://forum.church.tools/topic/11910)
- ChurchTools Academy: [Rechteverwaltung verstehen](https://churchtools.academy/de/help/rechteverwaltung/rechteverwaltung-verstehen/), [CORS](https://churchtools.academy/help/system-einstellungen/api/0-cors/)
- 30-Tage-Testinstanz: <https://church.tools/de/test-churchtools/> · Support: <support@churchtools.de>
